# Sanitized TokenPilot v0 fixture. Never use a real credential here.
TOKENPILOT_HOST=127.0.0.1
TOKENPILOT_PORT=4318
TOKENPILOT_EXPOSED=false
TOKENPILOT_API_TOKEN=fixture_owner_secret_must_not_enter_manifest
TOKENPILOT_PUBLIC_BASE_URL=
TOKENPILOT_RUNNER_INTERVAL=3
