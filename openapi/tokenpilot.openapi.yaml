openapi: 3.1.0
info:
  title: TokenPilot Local Control Plane API
  version: 0.1.0-alpha
  description: >
    Local-first automation surface for TokenPilot. Phase 2 supports ChatGPT direct-drive
    operations for small trusted edits plus asynchronous Codex jobs for complex work. The
    full HTTPS / Custom GPT Actions automation loop remains under validation.
servers:
  - url: https://tokenpilot.example.com
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
  schemas:
    ApiError:
      type: object
      additionalProperties: false
      properties:
        code:
          type: string
          description: Stable machine-readable error code.
        message:
          type: string
        hint:
          type: string
        details: {}
      required: [code, message]
    ApiErrorResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          enum: [false]
        error:
          $ref: '#/components/schemas/ApiError'
      required: [ok, error]
    HealthResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        mode:
          type: string
        authRequired:
          type: boolean
        exposed:
          type: boolean
        publicBaseUrl:
          oneOf:
            - type: string
            - type: "null"
        openapiUrl:
          type: string
      required: [ok, mode, authRequired, exposed, publicBaseUrl, openapiUrl]
    SetupStatusStep:
      type: object
      additionalProperties: false
      properties:
        key:
          type: string
          enum: [runtime, auth, oauth, repo, runner, gpt, firstTask]
        ok:
          type: boolean
        label:
          type: string
        detail:
          type: string
        nextAction:
          type: string
      required: [key, ok, label, detail, nextAction]
    SetupStatusResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          enum: [true]
        ready:
          type: boolean
        authRequired:
          type: boolean
        exposed:
          type: boolean
        publicBaseUrlConfigured:
          type: boolean
        oauthStatus:
          type: string
          enum: [disabled, ready, needs-attention]
        oauthProtectedResourceMetadataUrl:
          oneOf:
            - type: string
            - type: "null"
        openapiUrl:
          type: string
        runnerStatus:
          type: string
          enum: [missing, ready]
        firstTaskSeen:
          type: boolean
        steps:
          type: array
          items:
            $ref: '#/components/schemas/SetupStatusStep'
      required: [ok, ready, authRequired, exposed, publicBaseUrlConfigured, oauthStatus, oauthProtectedResourceMetadataUrl, openapiUrl, runnerStatus, firstTaskSeen, steps]
    GptConfig:
      type: object
      additionalProperties: false
      properties:
        version:
          type: string
          description: Product-facing display version, for example v0.1.0-alpha (53).
        productVersion:
          type: string
          description: Product release version from package metadata, for example v0.1.0-alpha.
        schemaVersion:
          type: string
          description: GPT instructions and OpenAPI schema revision shown in parentheses.
        buildVersion:
          type: string
          description: Git commit time version used as tooltip/debug detail, for example 26.0531.213301.
        updatedAt:
          type: string
        actionHost:
          type: string
        openapiUrl:
          type: string
        publicBaseUrl:
          oneOf:
            - type: string
            - type: "null"
        schemaImportUrl:
          type: string
        repoGovernance:
          $ref: '#/components/schemas/RepoGovernance'
        instructions:
          type: string
        notes:
          type: array
          items:
            type: string
      required: [version, productVersion, schemaVersion, buildVersion, updatedAt, actionHost, openapiUrl, publicBaseUrl, schemaImportUrl, repoGovernance, instructions, notes]
    RepoGovernance:
      type: object
      additionalProperties: false
      properties:
        defaultRepoId:
          type: string
        configScope:
          type: string
          enum: [local-private]
        pathVisibility:
          type: string
          enum: [hidden]
        repos:
          type: array
          items:
            $ref: '#/components/schemas/RepoGovernanceEntry'
      required: [defaultRepoId, configScope, pathVisibility, repos]
    RepoGovernanceEntry:
      type: object
      additionalProperties: false
      properties:
        repoId:
          type: string
        status:
          type: string
          enum: [enabled, missing, blocked]
        defaultRepo:
          type: boolean
        source:
          type: string
          enum: [default, default-sibling, local-config]
        pathConfigured:
          type: boolean
        allowlisted:
          type: boolean
        pathVisibility:
          type: string
          enum: [hidden]
        capabilities:
          type: array
          items:
            type: string
            enum: [pack, files-read, codex-run]
      required: [repoId, status, defaultRepo, source, pathConfigured, allowlisted, pathVisibility, capabilities]
    GptConfigResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        config:
          $ref: '#/components/schemas/GptConfig'
      required: [ok, config]
    GitCommitSummary:
      type: object
      additionalProperties: false
      properties:
        hash:
          type: string
        shortHash:
          type: string
        subject:
          type: string
        committedAt:
          type: string
      required: [hash, shortHash, subject, committedAt]
    ChatDirectExecutionMetadata:
      type: object
      additionalProperties: false
      properties:
        lane:
          type: string
          const: chat-direct
        modelLoopOwner:
          type: string
          const: chatgpt
        executionScope:
          type: string
          enum: [workspace, host]
        executor:
          type: string
          description: Selected Direct Drive executor id. Current built-in ids include tokenpilot-direct and codex-app-server-standalone; future downstream MCP executor ids remain dynamic.
        selectionMode:
          type: string
          enum: [automatic, explicit]
        operationId:
          type: string
        changedPaths:
          type: array
          items:
            type: string
        evidenceBundleId:
          oneOf:
            - type: string
            - type: "null"
        fallbackReason:
          type: string
      required: [lane, modelLoopOwner, executionScope, executor, selectionMode, operationId, changedPaths, evidenceBundleId]
    RecentCommitsResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        repoId:
          type: string
        commits:
          type: array
          items:
            $ref: '#/components/schemas/GitCommitSummary'
        execution:
          $ref: '#/components/schemas/ChatDirectExecutionMetadata'
      required: [ok, repoId, commits, execution]
    PackJobPayload:
      type: object
      additionalProperties: false
      properties:
        repoId:
          type: string
          description: Public repository alias resolved to a private local path by the operator. Defaults to tokenpilot in the current single-repo local-first setup.
    TaskPackJobPayload:
      type: object
      additionalProperties: false
      properties:
        title:
          type: string
        problem:
          type: string
        contextSummary:
          type: string
        mustInspect:
          type: array
          items:
            type: string
        mayInspect:
          type: array
          items:
            type: string
        mustNotModify:
          type: array
          items:
            type: string
        verificationCommands:
          type: array
          items:
            type: string
        acceptanceCriteria:
          type: array
          items:
            type: string
      required: [title, problem]
    CodexRunJobPayload:
      type: object
      additionalProperties: false
      properties:
        repoId:
          type: string
          description: Public repository alias resolved by the local allowlist. Defaults to tokenpilot.
        title:
          type: string
        instructions:
          type: string
          description: Task instructions passed to local Codex CLI.
        executionMode:
          type: string
          enum: [plan, review, develop]
          default: develop
          description: plan runs read-only planning and writes artifacts only; review skips the main exec step and reviews current changes; develop may modify the execution repo according to sandbox and worktree policy.
        worktreePolicy:
          type: string
          enum: [auto, always, never]
          default: auto
        branchName:
          type: string
        approvalPolicy:
          type: string
          enum: [untrusted, on-request, never]
          default: never
        sandbox:
          type: string
          enum: [read-only, workspace-write, danger-full-access]
          default: workspace-write
          description: Effective sandbox is forced to read-only for plan and review. develop defaults to workspace-write unless overridden.
        verificationCommands:
          type: array
          items:
            type: string
        acceptanceCriteria:
          type: array
          items:
            type: string
        commitPolicy:
          type: string
          enum: [none, propose, commit]
          default: propose
        commitTitle:
          type: string
        commitBody:
          type: string
      required: [title, instructions]
    PackJobRecord:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
          description: Unique job id
        type:
          type: string
          enum: [pack]
        status:
          type: string
          enum: [queued, running, completed, failed]
        createdAt:
          type: string
        updatedAt:
          type: string
        payload:
          $ref: '#/components/schemas/PackJobPayload'
        result:
          oneOf:
            - $ref: '#/components/schemas/PackJobResult'
            - type: 'null'
        error:
          type: string
      required: [id, type, status, createdAt, updatedAt, payload]
    TaskPackJobRecord:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
          description: Unique job id
        type:
          type: string
          enum: [taskpack]
        status:
          type: string
          enum: [queued, running, completed, failed]
        createdAt:
          type: string
        updatedAt:
          type: string
        payload:
          $ref: '#/components/schemas/TaskPackJobPublicPayload'
        result:
          oneOf:
            - $ref: '#/components/schemas/TaskPackJobResult'
            - type: 'null'
        error:
          type: string
      required: [id, type, status, createdAt, updatedAt, payload]
    CodexRunJobRecord:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
          description: Unique job id
        type:
          type: string
          enum: [codex-run]
        status:
          type: string
          enum: [queued, running, completed, failed]
        createdAt:
          type: string
        updatedAt:
          type: string
        payload:
          $ref: '#/components/schemas/CodexRunJobPublicPayload'
        result:
          oneOf:
            - $ref: '#/components/schemas/CodexRunJobResult'
            - type: 'null'
        error:
          type: string
      required: [id, type, status, createdAt, updatedAt, payload]
    PackJobResult:
      type: object
      additionalProperties: false
      properties:
        createdAt:
          type: string
        repoId:
          type: string
        repoName:
          type: string
        repomixXmlPath:
          type: string
        promptPath:
          type: string
        summaryPath:
          type: string
        manifestPath:
          type: string
        publicIncludeEntries:
          type: array
          items:
            type: string
      required:
        [createdAt, repoId, repoName, repomixXmlPath, promptPath, summaryPath, manifestPath, publicIncludeEntries]
    TaskPackJobPublicPayload:
      type: object
      additionalProperties: false
      properties:
        title:
          type: string
      required: [title]
    CodexRunJobPublicPayload:
      type: object
      additionalProperties: false
      properties:
        repoId:
          type: string
        title:
          type: string
        executionMode:
          type: string
          enum: [plan, review, develop]
        worktreePolicy:
          type: string
          enum: [auto, always, never]
        commitPolicy:
          type: string
          enum: [none, propose, commit]
      required: [repoId, title]
    TaskPackJobResult:
      type: object
      additionalProperties: false
      properties:
        createdAt:
          type: string
        title:
          type: string
        markdownPath:
          type: string
        jsonPath:
          type: string
      required: [createdAt, title, markdownPath, jsonPath]
    CodexRunCommitResult:
      type: object
      additionalProperties: false
      properties:
        committed:
          type: boolean
        commitHash:
          type: string
        commitMessage:
          type: string
        error:
          type: string
      required: [committed]
    CodexRunJobResult:
      type: object
      additionalProperties: false
      properties:
        createdAt:
          type: string
        repoId:
          type: string
        title:
          type: string
        executionMode:
          type: string
          enum: [plan, review, develop]
        worktreePolicy:
          type: string
          enum: [auto, always, never]
        worktreeCreated:
          type: boolean
        branchName:
          type: string
        statusSummary:
          type: string
        codexExitCode:
          type: integer
        reviewExitCode:
          type: integer
        gitStatus:
          type: string
        hasDiff:
          type: boolean
        commit:
          $ref: '#/components/schemas/CodexRunCommitResult'
        promptPath:
          type: string
        stdoutPath:
          type: string
        stderrPath:
          type: string
        diffPath:
          type: string
        reviewPath:
          type: string
        summaryPath:
          type: string
      required:
        [createdAt, repoId, title, executionMode, worktreePolicy, worktreeCreated, statusSummary, codexExitCode, reviewExitCode, gitStatus, hasDiff, commit, promptPath, stdoutPath, stderrPath, diffPath, reviewPath, summaryPath]
    HostDirectRoot:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        displayName:
          type: string
        access:
          type: array
          items:
            type: string
            enum: [read, write]
      required: [id, displayName, access]
    HostDirectRootsResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        executionScope:
          type: string
          const: host
        mode:
          type: string
          enum: [read-only, mutation-enabled]
        roots:
          type: array
          items:
            $ref: '#/components/schemas/HostDirectRoot'
      required: [ok, executionScope, mode, roots]
    HostFileReadPayload:
      type: object
      additionalProperties: false
      properties:
        rootId:
          type: string
          description: Public-safe Host Direct root alias. This is not a local absolute path.
        path:
          type: string
          description: Relative text-like file path inside the selected Host Direct root. Absolute paths are rejected.
        executorId:
          type: string
          description: Optional explicit Host Direct executor id. Explicit selection never silently falls back to another executor.
      required: [rootId, path]
    HostFileReadResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        rootId:
          type: string
        file:
          $ref: '#/components/schemas/FileReadResult'
        execution:
          $ref: '#/components/schemas/ChatDirectExecutionMetadata'
      required: [ok, rootId, file, execution]
    HostMutationWritePayload:
      type: object
      additionalProperties: false
      properties:
        operation:
          type: string
          const: files.write
        rootId:
          type: string
        path:
          type: string
          description: Relative text-like path inside the selected Host Root alias. Absolute paths are rejected.
        content:
          type: string
          maxLength: 65536
        sessionId:
          type: string
          description: Required when the canonical target re-enters a registered Workspace.
        executorId:
          type: string
          description: Optional explicit executor. Execution is pinned to the executor stored on the approval.
      required: [operation, rootId, path, content]
    HostMutationEditPayload:
      type: object
      additionalProperties: false
      properties:
        operation:
          type: string
          const: files.edit
        rootId:
          type: string
        path:
          type: string
          description: Relative text-like path inside the selected Host Root alias. Absolute paths are rejected.
        oldText:
          type: string
          minLength: 1
          maxLength: 65536
          description: Exact text that must occur exactly once before execution.
        newText:
          type: string
          maxLength: 65536
        sessionId:
          type: string
          description: Required when the canonical target re-enters a registered Workspace.
        executorId:
          type: string
          description: Optional explicit executor. Execution is pinned to the executor stored on the approval.
      required: [operation, rootId, path, oldText, newText]
    HostMutationPreparePayload:
      oneOf:
        - allOf:
            - $ref: '#/components/schemas/HostMutationWritePayload'
            - type: object
              required: [idempotencyKey]
              properties:
                idempotencyKey:
                  type: string
                  minLength: 8
                  maxLength: 128
        - allOf:
            - $ref: '#/components/schemas/HostMutationEditPayload'
            - type: object
              required: [idempotencyKey]
              properties:
                idempotencyKey:
                  type: string
                  minLength: 8
                  maxLength: 128
    HostMutationDecisionPayload:
      type: object
      additionalProperties: false
      properties:
        approvalId:
          type: string
        expectedRevision:
          type: integer
          minimum: 1
        decision:
          type: string
          enum: [approved, denied]
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
      required: [approvalId, expectedRevision, decision, idempotencyKey]
    HostMutationExecutePayload:
      oneOf:
        - allOf:
            - $ref: '#/components/schemas/HostMutationWritePayload'
            - type: object
              required: [approvalId, expectedApprovalRevision, idempotencyKey]
              properties:
                approvalId:
                  type: string
                expectedApprovalRevision:
                  type: integer
                  minimum: 1
                idempotencyKey:
                  type: string
                  minLength: 8
                  maxLength: 128
        - allOf:
            - $ref: '#/components/schemas/HostMutationEditPayload'
            - type: object
              required: [approvalId, expectedApprovalRevision, idempotencyKey]
              properties:
                approvalId:
                  type: string
                expectedApprovalRevision:
                  type: integer
                  minimum: 1
                idempotencyKey:
                  type: string
                  minLength: 8
                  maxLength: 128
    HostMutationApproval:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        operation:
          type: string
          enum: [files.write, files.edit]
        rootId:
          type: string
        relativePath:
          type: string
        mutationHash:
          type: string
          pattern: '^[a-f0-9]{64}$'
        executorId:
          type: string
        scope:
          type: string
          const: host
        targetKind:
          type: string
          enum: [workspace, pure-host]
        workspaceId:
          oneOf:
            - type: string
            - type: "null"
        repoId:
          oneOf:
            - type: string
            - type: "null"
        sessionId:
          oneOf:
            - type: string
            - type: "null"
        status:
          type: string
          enum: [pending, approved, denied, consumed, expired]
        publicSummary:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        decidedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        consumedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        revision:
          type: integer
          minimum: 1
      required: [id, operation, rootId, relativePath, mutationHash, executorId, scope, targetKind, workspaceId, repoId, sessionId, status, publicSummary, createdAt, expiresAt, decidedAt, consumedAt, revision]
    HostMutationApprovalResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        approval:
          $ref: '#/components/schemas/HostMutationApproval'
        replayed:
          type: boolean
      required: [ok, approval, replayed]
    HostMutationEvidence:
      oneOf:
        - type: object
          additionalProperties: false
          properties:
            kind:
              type: string
              const: pure-host-audit
            auditId:
              type: string
          required: [kind, auditId]
        - type: object
          additionalProperties: false
          properties:
            kind:
              type: string
              const: task-evidence
            bundleId:
              type: string
            itemId:
              type: string
          required: [kind, bundleId, itemId]
    HostMutationExecuteResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        operation:
          type: string
          enum: [files.write, files.edit]
        rootId:
          type: string
        path:
          type: string
          description: Public-safe Host Root alias path, never a local absolute path.
        beforeHash:
          oneOf:
            - type: string
            - type: "null"
        afterHash:
          type: string
        approval:
          type: object
          additionalProperties: false
          properties:
            id:
              type: string
            status:
              type: string
              const: consumed
          required: [id, status]
        execution:
          $ref: '#/components/schemas/ChatDirectExecutionMetadata'
        evidence:
          $ref: '#/components/schemas/HostMutationEvidence'
        replayed:
          type: boolean
      required: [ok, operation, rootId, path, beforeHash, afterHash, approval, execution, evidence, replayed]
    HostCommandRequestPayload:
      type: object
      additionalProperties: false
      properties:
        rootId:
          type: string
          description: Public-safe Host Root alias.
        workdir:
          type: string
          description: Relative working directory inside the Host Root. Defaults to the root.
        command:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[A-Za-z0-9._+-]+$'
          description: Structured executable name only; raw shell source is not accepted.
        args:
          type: array
          maxItems: 64
          items:
            type: string
            minLength: 1
            maxLength: 2048
        timeoutMs:
          type: integer
          minimum: 250
          maximum: 15000
          default: 5000
        sessionId:
          type: string
          description: Required for write-effect commands that re-enter a registered Workspace.
        executorId:
          type: string
          description: Optional explicit Host executor; approved execution remains pinned to this executor.
      required: [rootId, command]
    HostCommandPreparePayload:
      allOf:
        - $ref: '#/components/schemas/HostCommandRequestPayload'
        - type: object
          required: [idempotencyKey]
          properties:
            idempotencyKey:
              type: string
              minLength: 8
              maxLength: 128
    HostCommandDecisionPayload:
      type: object
      additionalProperties: false
      properties:
        approvalId:
          type: string
        expectedRevision:
          type: integer
          minimum: 1
        decision:
          type: string
          enum: [approved, denied]
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
      required: [approvalId, expectedRevision, decision, idempotencyKey]
    HostCommandExecutePayload:
      allOf:
        - $ref: '#/components/schemas/HostCommandRequestPayload'
        - type: object
          required: [approvalId, expectedApprovalRevision, idempotencyKey]
          properties:
            approvalId:
              type: string
            expectedApprovalRevision:
              type: integer
              minimum: 1
            idempotencyKey:
              type: string
              minLength: 8
              maxLength: 128
    HostCommandApproval:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        rootId:
          type: string
        workdir:
          type: string
        command:
          type: string
        args:
          type: array
          items:
            type: string
        commandHash:
          type: string
          pattern: '^[a-f0-9]{64}$'
        effect:
          type: string
          enum: [read, write]
        timeoutMs:
          type: integer
        executorId:
          type: string
        targetKind:
          type: string
          enum: [workspace, pure-host]
        workspaceId:
          oneOf:
            - type: string
            - type: "null"
        repoId:
          oneOf:
            - type: string
            - type: "null"
        sessionId:
          oneOf:
            - type: string
            - type: "null"
        status:
          type: string
          enum: [pending, approved, denied, consumed, expired]
        publicSummary:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        decidedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        consumedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        revision:
          type: integer
          minimum: 1
      required: [id, rootId, workdir, command, args, commandHash, effect, timeoutMs, executorId, targetKind, workspaceId, repoId, sessionId, status, publicSummary, createdAt, expiresAt, decidedAt, consumedAt, revision]
    HostCommandApprovalResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        approval:
          $ref: '#/components/schemas/HostCommandApproval'
        replayed:
          type: boolean
      required: [ok, approval, replayed]
    HostCommandEvidence:
      oneOf:
        - type: object
          additionalProperties: false
          properties:
            kind:
              type: string
              const: direct-command-audit
            auditId:
              type: string
          required: [kind, auditId]
        - type: object
          additionalProperties: false
          properties:
            kind:
              type: string
              const: task-evidence
            bundleId:
              type: string
            itemId:
              type: string
          required: [kind, bundleId, itemId]
    HostCommandExecuteResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        rootId:
          type: string
        workdir:
          type: string
          description: Public-safe Host Root alias path, never a local absolute path.
        command:
          type: string
        effect:
          type: string
          enum: [read, write]
        exitCode:
          oneOf:
            - type: integer
            - type: "null"
        output:
          type: string
          description: Bounded merged process output after TokenPilot public-safe projection.
        truncated:
          type: boolean
        timedOut:
          type: boolean
        errorCode:
          oneOf:
            - type: string
            - type: "null"
        approval:
          type: object
          additionalProperties: false
          properties:
            id:
              type: string
            status:
              type: string
              const: consumed
          required: [id, status]
        execution:
          $ref: '#/components/schemas/ChatDirectExecutionMetadata'
        evidence:
          $ref: '#/components/schemas/HostCommandEvidence'
        replayed:
          type: boolean
      required: [ok, rootId, workdir, command, effect, exitCode, output, truncated, timedOut, errorCode, approval, execution, evidence, replayed]
    HostProcessStartRequestPayload:
      type: object
      additionalProperties: false
      properties:
        operation:
          type: string
          const: start
        rootId:
          type: string
          description: Public-safe Host Root alias containing the registered Workspace.
        workdir:
          type: string
          description: Relative working directory inside the Host Root.
        command:
          type: string
          minLength: 1
          maxLength: 128
          pattern: '^[A-Za-z0-9._+-]+$'
          description: Structured executable name only; raw shell source is rejected.
        args:
          type: array
          maxItems: 64
          items:
            type: string
            minLength: 1
            maxLength: 2048
        sessionId:
          type: string
          description: Owning chat-direct development Session. Managed Process start always requires its Writer Lease.
        executorId:
          type: string
        startupTimeoutMs:
          type: integer
          minimum: 100
          maximum: 5000
          default: 1000
      required: [operation, rootId, command, sessionId]
    HostProcessInputRequestPayload:
      type: object
      additionalProperties: false
      properties:
        operation:
          type: string
          const: input
        processId:
          type: string
          pattern: '^host_process_[A-Za-z0-9_-]{8,160}$'
        sessionId:
          type: string
        input:
          type: string
          minLength: 1
          maxLength: 8192
          description: Transient input. TokenPilot persists only its SHA-256 and byte count, not the raw value.
        waitForPrompt:
          type: boolean
          default: true
        timeoutMs:
          type: integer
          minimum: 100
          maximum: 8000
          default: 2000
      required: [operation, processId, sessionId, input]
    HostProcessStopRequestPayload:
      type: object
      additionalProperties: false
      properties:
        operation:
          type: string
          const: stop
        processId:
          type: string
          pattern: '^host_process_[A-Za-z0-9_-]{8,160}$'
        sessionId:
          type: string
      required: [operation, processId, sessionId]
    HostProcessPreparePayload:
      oneOf:
        - allOf:
            - $ref: '#/components/schemas/HostProcessStartRequestPayload'
            - type: object
              required: [idempotencyKey]
              properties:
                idempotencyKey:
                  type: string
                  minLength: 8
                  maxLength: 128
        - allOf:
            - $ref: '#/components/schemas/HostProcessInputRequestPayload'
            - type: object
              required: [idempotencyKey]
              properties:
                idempotencyKey:
                  type: string
                  minLength: 8
                  maxLength: 128
        - allOf:
            - $ref: '#/components/schemas/HostProcessStopRequestPayload'
            - type: object
              required: [idempotencyKey]
              properties:
                idempotencyKey:
                  type: string
                  minLength: 8
                  maxLength: 128
    HostProcessDecisionPayload:
      type: object
      additionalProperties: false
      properties:
        approvalId:
          type: string
        expectedRevision:
          type: integer
          minimum: 1
        decision:
          type: string
          enum: [approved, denied]
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
      required: [approvalId, expectedRevision, decision, idempotencyKey]
    HostProcessExecutePayload:
      oneOf:
        - allOf:
            - $ref: '#/components/schemas/HostProcessStartRequestPayload'
            - type: object
              required: [approvalId, expectedApprovalRevision, idempotencyKey]
              properties:
                approvalId:
                  type: string
                expectedApprovalRevision:
                  type: integer
                  minimum: 1
                idempotencyKey:
                  type: string
                  minLength: 8
                  maxLength: 128
        - allOf:
            - $ref: '#/components/schemas/HostProcessInputRequestPayload'
            - type: object
              required: [approvalId, expectedApprovalRevision, idempotencyKey]
              properties:
                approvalId:
                  type: string
                expectedApprovalRevision:
                  type: integer
                  minimum: 1
                idempotencyKey:
                  type: string
                  minLength: 8
                  maxLength: 128
        - allOf:
            - $ref: '#/components/schemas/HostProcessStopRequestPayload'
            - type: object
              required: [approvalId, expectedApprovalRevision, idempotencyKey]
              properties:
                approvalId:
                  type: string
                expectedApprovalRevision:
                  type: integer
                  minimum: 1
                idempotencyKey:
                  type: string
                  minLength: 8
                  maxLength: 128
    HostProcessReadPayload:
      type: object
      additionalProperties: false
      properties:
        processId:
          type: string
          pattern: '^host_process_[A-Za-z0-9_-]{8,160}$'
        offset:
          type: integer
          minimum: 0
        length:
          type: integer
          minimum: 1
          maximum: 1000
        waitMs:
          type: integer
          minimum: 0
          maximum: 5000
      required: [processId]
    HostProcessApproval:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        operation:
          type: string
          enum: [start, input, stop]
        processId:
          oneOf:
            - type: string
            - type: "null"
        actionHash:
          type: string
          pattern: '^[a-f0-9]{64}$'
        rootId:
          oneOf:
            - type: string
            - type: "null"
        workdir:
          oneOf:
            - type: string
            - type: "null"
        command:
          oneOf:
            - type: string
            - type: "null"
        workspaceId:
          type: string
        repoId:
          type: string
        sessionId:
          type: string
        writerLeaseId:
          oneOf:
            - type: string
            - type: "null"
        executorId:
          type: string
        inputHash:
          oneOf:
            - type: string
              pattern: '^[a-f0-9]{64}$'
            - type: "null"
        inputBytes:
          oneOf:
            - type: integer
              minimum: 0
            - type: "null"
        status:
          type: string
          enum: [pending, approved, denied, consumed, expired]
        publicSummary:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        decidedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        consumedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        revision:
          type: integer
          minimum: 1
      required: [id, operation, processId, actionHash, rootId, workdir, command, workspaceId, repoId, sessionId, writerLeaseId, executorId, inputHash, inputBytes, status, publicSummary, createdAt, expiresAt, decidedAt, consumedAt, revision]
    HostProcessApprovalResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        approval:
          $ref: '#/components/schemas/HostProcessApproval'
        replayed:
          type: boolean
      required: [ok, approval, replayed]
    HostProcessRecord:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
          pattern: '^host_process_[A-Za-z0-9_-]{8,160}$'
        rootId:
          type: string
        workdir:
          type: string
          description: Public-safe Host Root alias path, never a local absolute path.
        command:
          type: string
        status:
          type: string
          enum: [starting, running, exited, terminated, failed, stale]
        workspaceId:
          type: string
        repoId:
          type: string
        sessionId:
          type: string
        executorId:
          type: string
        startedAt:
          type: string
          format: date-time
        completedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        exitCode:
          oneOf:
            - type: integer
            - type: "null"
        revision:
          type: integer
          minimum: 1
      required: [id, rootId, workdir, command, status, workspaceId, repoId, sessionId, executorId, startedAt, completedAt, exitCode, revision]
    HostProcessEvidence:
      type: object
      additionalProperties: false
      properties:
        kind:
          type: string
          const: task-evidence
        bundleId:
          type: string
        itemId:
          type: string
      required: [kind, bundleId, itemId]
    HostProcessActionResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        operation:
          type: string
          enum: [start, input, stop]
        process:
          $ref: '#/components/schemas/HostProcessRecord'
        errorCode:
          oneOf:
            - type: string
            - type: "null"
        approval:
          type: object
          additionalProperties: false
          properties:
            id:
              type: string
            status:
              type: string
              const: consumed
          required: [id, status]
        execution:
          $ref: '#/components/schemas/ChatDirectExecutionMetadata'
        evidence:
          $ref: '#/components/schemas/HostProcessEvidence'
        auditId:
          type: string
        replayed:
          type: boolean
      required: [ok, operation, process, errorCode, approval, evidence, auditId, replayed]
    HostProcessReadResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        process:
          $ref: '#/components/schemas/HostProcessRecord'
        output:
          type: string
          description: Bounded public-safe process output. Mutation responses intentionally do not carry or persist stdout.
        truncated:
          type: boolean
      required: [ok, process, output, truncated]
    HostProcessListResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        processes:
          type: array
          items:
            $ref: '#/components/schemas/HostProcessRecord'
      required: [ok, processes]
    FileReadPayload:
      type: object
      additionalProperties: false
      properties:
        repoId:
          type: string
        executorId:
          type: string
          description: Optional explicit Direct Drive executor id. When set, TokenPilot will not silently fall back to another executor.
        path:
          type: string
          description: Relative text file path inside the mapped repository.
        offset:
          type: integer
          minimum: 0
          description: Optional byte offset for chunked reading.
        limit:
          type: integer
          minimum: 1
          description: Optional maximum byte count for chunked reading.
      required: [repoId, path]
    FileReadBatchPayload:
      type: object
      additionalProperties: false
      properties:
        repoId:
          type: string
        executorId:
          type: string
          description: Optional explicit Direct Drive executor id. When set, TokenPilot will not silently fall back to another executor.
        paths:
          type: array
          items:
            type: string
          minItems: 1
          maxItems: 10
        offset:
          type: integer
          minimum: 0
          description: Optional byte offset applied to each returned file for chunked reading.
        limit:
          type: integer
          minimum: 1
          description: Optional maximum byte count applied to each returned file.
      required: [repoId, paths]
    FileReadResult:
      type: object
      additionalProperties: false
      properties:
        path:
          type: string
        content:
          type: string
        truncated:
          type: boolean
        size:
          type: integer
        encoding:
          type: string
        returnedBytes:
          type: integer
        maxBytes:
          type: integer
        previewMode:
          type: string
          enum: [head]
        offset:
          type: integer
        nextOffset:
          oneOf:
            - type: integer
            - type: "null"
        eof:
          type: boolean
      required: [path, content, truncated, size, encoding, returnedBytes, maxBytes, previewMode, offset, nextOffset, eof]
    JobArtifactSummary:
      type: object
      additionalProperties: false
      properties:
        key:
          type: string
          enum: [repomixXml, prompt, summary, manifest, markdown, json, codexPrompt, codexStdout, codexStderr, codexDiff, codexReview, codexSummary]
        label:
          type: string
        path:
          type: string
        contentType:
          type: string
      required: [key, label, path, contentType]
    JobArtifactReadResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        artifact:
          $ref: '#/components/schemas/JobArtifactSummary'
        file:
          $ref: '#/components/schemas/FileReadResult'
      required: [ok, artifact, file]
    JobArtifactsListResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        artifacts:
          type: array
          items:
            $ref: '#/components/schemas/JobArtifactSummary'
      required: [ok, artifacts]
    ReadFileResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        repoId:
          type: string
        file:
          $ref: '#/components/schemas/FileReadResult'
        execution:
          $ref: '#/components/schemas/ChatDirectExecutionMetadata'
      required: [ok, repoId, file, execution]
    ReadFilesResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        repoId:
          type: string
        files:
          type: array
          items:
            $ref: '#/components/schemas/FileReadResult'
        execution:
          $ref: '#/components/schemas/ChatDirectExecutionMetadata'
      required: [ok, repoId, files, execution]
    FileWritePayload:
      type: object
      additionalProperties: false
      properties:
        repoId:
          type: string
          default: tokenpilot
        executorId:
          type: string
          description: Optional explicit Direct Drive executor id. When set, TokenPilot will not silently fall back to another executor.
        sessionId:
          type: string
          minLength: 1
          maxLength: 160
          description: Active chat-direct development session that owns the workspace writer lease.
        path:
          type: string
          description: Relative file path inside the mapped repository.
        content:
          type: string
          description: Full text content to write.
      required: [sessionId, path, content]
    FileWriteResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        repoId:
          type: string
        path:
          type: string
        written:
          type: boolean
        size:
          type: integer
        error:
          type: string
        execution:
          $ref: '#/components/schemas/ChatDirectExecutionMetadata'
      required: [ok, repoId, path, written, size, execution]
    FileEditPayload:
      type: object
      additionalProperties: false
      properties:
        repoId:
          type: string
          default: tokenpilot
        executorId:
          type: string
          description: Optional explicit Direct Drive executor id. When set, TokenPilot will not silently fall back to another executor.
        sessionId:
          type: string
          minLength: 1
          maxLength: 160
          description: Active chat-direct development session that owns the workspace writer lease.
        path:
          type: string
        search:
          type: string
          description: Exact text to find (must be unique in the file).
        replace:
          type: string
          description: Text to substitute in place of search.
      required: [sessionId, path, search, replace]
    FileEditResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        repoId:
          type: string
        path:
          type: string
        applied:
          type: boolean
        error:
          type: string
        execution:
          $ref: '#/components/schemas/ChatDirectExecutionMetadata'
      required: [ok, repoId, path, applied, execution]
    FileListPayload:
      type: object
      additionalProperties: false
      properties:
        repoId:
          type: string
          default: tokenpilot
        executorId:
          type: string
          description: Optional explicit Direct Drive executor id. When set, TokenPilot will not silently fall back to another executor.
        path:
          type: string
          default: "."
          description: Relative directory path inside the mapped repository.
      required: [path]
    FileListEntry:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
        type:
          type: string
          enum: [file, directory]
        size:
          type: integer
      required: [name, type]
    FileListResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        repoId:
          type: string
        path:
          type: string
        entries:
          type: array
          items:
            $ref: '#/components/schemas/FileListEntry'
        error:
          type: string
        execution:
          $ref: '#/components/schemas/ChatDirectExecutionMetadata'
      required: [ok, repoId, path, entries, execution]
    SearchPayload:
      type: object
      additionalProperties: false
      properties:
        repoId:
          type: string
          default: tokenpilot
        executorId:
          type: string
          description: Optional explicit Direct Drive executor id. When set, TokenPilot will not silently fall back to another executor.
        pattern:
          type: string
          description: Search pattern (regex-compatible).
        path:
          type: string
          description: Optional subdirectory to limit search scope.
        maxResults:
          type: integer
          minimum: 1
          maximum: 40
          default: 40
        contextLines:
          type: integer
          minimum: 0
          maximum: 3
          default: 0
        caseSensitive:
          type: boolean
          default: false
      required: [pattern]
    SearchMatch:
      type: object
      additionalProperties: false
      properties:
        path:
          type: string
        line:
          type: integer
        content:
          type: string
      required: [path, line, content]
    SearchResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        repoId:
          type: string
        pattern:
          type: string
        matches:
          type: array
          items:
            $ref: '#/components/schemas/SearchMatch'
        truncated:
          type: boolean
        totalMatches:
          type: integer
        error:
          type: string
        execution:
          $ref: '#/components/schemas/ChatDirectExecutionMetadata'
      required: [ok, repoId, pattern, matches, truncated, totalMatches, execution]
    ShellRunPayload:
      type: object
      additionalProperties: false
      properties:
        repoId:
          type: string
          default: tokenpilot
        executorId:
          type: string
          description: Optional explicit Direct Drive executor id. When set, TokenPilot will not silently fall back to another executor.
        sessionId:
          type: string
          minLength: 1
          maxLength: 160
          description: Required when policy classifies the command as potentially mutating; the session must own the active writer lease. Read-only commands may omit it.
        command:
          type: string
          description: Whitelisted command name. node, python, python3, npx, tsx, and make are high-trust commands and are blocked in exposed mode unless the operator explicitly sets TOKENPILOT_ALLOW_HIGH_TRUST_COMMANDS=true.
        args:
          type: array
          items:
            type: string
          description: Command arguments.
        workdir:
          type: string
          description: Optional working directory relative to repo root.
      required: [command, args]
    ShellRunResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        exitCode:
          type: integer
        stdout:
          type: string
        stderr:
          type: string
        truncated:
          type: boolean
        executedCommand:
          type: string
        error:
          type: string
        execution:
          $ref: '#/components/schemas/ChatDirectExecutionMetadata'
      required: [ok, exitCode, stdout, stderr, truncated, executedCommand, execution]
    GitDiffResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        repoId:
          type: string
        diff:
          type: string
        truncated:
          type: boolean
        error:
          type: string
        execution:
          $ref: '#/components/schemas/ChatDirectExecutionMetadata'
      required: [ok, repoId, diff, truncated, execution]
    GitStatusEntry:
      type: object
      additionalProperties: false
      properties:
        path:
          type: string
        status:
          type: string
        staged:
          type: boolean
      required: [path, status, staged]
    GitStatusResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        repoId:
          type: string
        branch:
          type: string
        entries:
          type: array
          items:
            $ref: '#/components/schemas/GitStatusEntry'
        error:
          type: string
        execution:
          $ref: '#/components/schemas/ChatDirectExecutionMetadata'
      required: [ok, repoId, branch, entries, execution]
    GitCommitPayload:
      type: object
      additionalProperties: false
      properties:
        repoId:
          type: string
          default: tokenpilot
        executorId:
          type: string
          description: Optional explicit Direct Drive executor id. When set, TokenPilot will not silently fall back to another executor.
        sessionId:
          type: string
          minLength: 1
          maxLength: 160
          description: Active chat-direct development session that owns the workspace writer lease.
        message:
          type: string
          description: Commit title / subject line.
        body:
          type: string
          description: Optional commit body (extended description).
      required: [sessionId, message]
    GitCommitResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        repoId:
          type: string
        committed:
          type: boolean
        commitHash:
          type: string
        commitMessage:
          type: string
        error:
          type: string
        execution:
          $ref: '#/components/schemas/ChatDirectExecutionMetadata'
      required: [ok, repoId, committed, execution]
    PackJobSummary:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        type:
          type: string
          enum: [pack]
        status:
          type: string
          enum: [queued, running, completed, failed]
        createdAt:
          type: string
        updatedAt:
          type: string
        headline:
          type: string
        hasResult:
          type: boolean
        hasError:
          type: boolean
        payload:
          $ref: '#/components/schemas/PackJobPayload'
        process:
          oneOf:
            - $ref: '#/components/schemas/TrackedJobProcess'
            - type: 'null'
        artifacts:
          type: array
          items:
            $ref: '#/components/schemas/JobArtifactSummary'
        result:
          oneOf:
            - $ref: '#/components/schemas/PackJobResult'
            - type: 'null'
        error:
          type: string
      required: [id, type, status, createdAt, updatedAt, headline, hasResult, hasError, payload]
    TaskPackJobSummary:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        type:
          type: string
          enum: [taskpack]
        status:
          type: string
          enum: [queued, running, completed, failed]
        createdAt:
          type: string
        updatedAt:
          type: string
        headline:
          type: string
        hasResult:
          type: boolean
        hasError:
          type: boolean
        payload:
          $ref: '#/components/schemas/TaskPackJobPublicPayload'
        process:
          oneOf:
            - $ref: '#/components/schemas/TrackedJobProcess'
            - type: 'null'
        artifacts:
          type: array
          items:
            $ref: '#/components/schemas/JobArtifactSummary'
        result:
          oneOf:
            - $ref: '#/components/schemas/TaskPackJobResult'
            - type: 'null'
        error:
          type: string
      required: [id, type, status, createdAt, updatedAt, headline, hasResult, hasError, payload]
    CodexRunJobSummary:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        type:
          type: string
          enum: [codex-run]
        status:
          type: string
          enum: [queued, running, completed, failed]
        createdAt:
          type: string
        updatedAt:
          type: string
        headline:
          type: string
        hasResult:
          type: boolean
        hasError:
          type: boolean
        payload:
          $ref: '#/components/schemas/CodexRunJobPublicPayload'
        process:
          oneOf:
            - $ref: '#/components/schemas/TrackedJobProcess'
            - type: 'null'
        artifacts:
          type: array
          items:
            $ref: '#/components/schemas/JobArtifactSummary'
        result:
          oneOf:
            - $ref: '#/components/schemas/CodexRunJobResult'
            - type: 'null'
        error:
          type: string
      required: [id, type, status, createdAt, updatedAt, headline, hasResult, hasError, payload]
    TrackedJobProcess:
      type: object
      additionalProperties: false
      properties:
        state:
          type: string
          enum: [running, paused, terminated, completed, failed]
        updatedAt:
          type: string
        label:
          type: string
      required: [state, updatedAt, label]
    CreatePackResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        job:
          $ref: '#/components/schemas/PackJobRecord'
      required: [ok, job]
    CreateTaskPackResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        job:
          $ref: '#/components/schemas/TaskPackJobRecord'
      required: [ok, job]
    CreateCodexRunResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        job:
          $ref: '#/components/schemas/CodexRunJobRecord'
      required: [ok, job]
    JobControlResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        jobId:
          type: string
        action:
          type: string
        state:
          type: string
        message:
          type: string
      required: [ok, jobId, action, state, message]
    TerminatedJobProcess:
      type: object
      additionalProperties: false
      properties:
        jobId:
          type: string
        state:
          type: string
        message:
          type: string
      required: [jobId, state, message]
    TerminateAllJobsResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        terminated:
          type: array
          items:
            $ref: '#/components/schemas/TerminatedJobProcess'
      required: [ok, terminated]
    JobsListResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        jobs:
          type: array
          items:
            oneOf:
              - $ref: '#/components/schemas/PackJobSummary'
              - $ref: '#/components/schemas/TaskPackJobSummary'
              - $ref: '#/components/schemas/CodexRunJobSummary'
        nextCursor:
          oneOf:
            - type: string
            - type: "null"
        totalVisible:
          type: integer
        includeResult:
          type: boolean
      required: [ok, jobs, nextCursor, totalVisible, includeResult]
    GetJobResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
        job:
          oneOf:
            - $ref: '#/components/schemas/PackJobSummary'
            - $ref: '#/components/schemas/TaskPackJobSummary'
            - $ref: '#/components/schemas/CodexRunJobSummary'
      required: [ok, job]
    ContinuityReadResponse:
      type: object
      additionalProperties: true
      properties:
        ok:
          type: boolean
          const: true
      required: [ok]
    ContinuityMutationResponse:
      type: object
      additionalProperties: true
      properties:
        ok:
          type: boolean
          const: true
        replayed:
          type: boolean
      required: [ok, replayed]
    DevelopmentDocumentRecord:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        projectId:
          type: string
        workspaceId:
          type: string
        kind:
          type: string
          enum: [spec, plan]
        title:
          type: string
        status:
          type: string
          enum: [draft, ready, approved, superseded, archived]
        currentVersion:
          type: integer
          minimum: 1
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        revision:
          type: integer
          minimum: 1
      required: [id, projectId, workspaceId, kind, title, status, currentVersion, createdAt, updatedAt, revision]
    DevelopmentDocumentVersionSummary:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        documentId:
          type: string
        version:
          type: integer
          minimum: 1
        contentHash:
          type: string
          pattern: '^[a-f0-9]{64}$'
        changeSummary:
          type: string
        createdAt:
          type: string
          format: date-time
      required: [id, documentId, version, contentHash, changeSummary, createdAt]
    DevelopmentDocumentVersionRecord:
      allOf:
        - $ref: '#/components/schemas/DevelopmentDocumentVersionSummary'
        - type: object
          additionalProperties: false
          properties:
            contentMarkdown:
              type: string
          required: [contentMarkdown]
    DevelopmentDocumentSummary:
      type: object
      additionalProperties: false
      properties:
        document:
          $ref: '#/components/schemas/DevelopmentDocumentRecord'
        currentVersion:
          $ref: '#/components/schemas/DevelopmentDocumentVersionSummary'
      required: [document, currentVersion]
    DevelopmentDocumentDetail:
      type: object
      additionalProperties: false
      properties:
        document:
          $ref: '#/components/schemas/DevelopmentDocumentRecord'
        currentVersion:
          $ref: '#/components/schemas/DevelopmentDocumentVersionSummary'
        currentContent:
          $ref: '#/components/schemas/DevelopmentDocumentVersionRecord'
        versions:
          type: array
          items:
            $ref: '#/components/schemas/DevelopmentDocumentVersionSummary'
      required: [document, currentVersion, currentContent, versions]
    DevelopmentDocumentCreatePayload:
      type: object
      additionalProperties: false
      properties:
        projectId:
          type: string
        workspaceId:
          type: string
        kind:
          type: string
          enum: [spec, plan]
        title:
          type: string
          minLength: 1
          maxLength: 240
        contentMarkdown:
          type: string
          minLength: 1
          maxLength: 250000
        changeSummary:
          type: string
          maxLength: 4000
          default: Initial version
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [projectId, workspaceId, kind, title, contentMarkdown, idempotencyKey]
    DevelopmentDocumentAppendVersionPayload:
      type: object
      additionalProperties: false
      properties:
        documentId:
          type: string
        contentMarkdown:
          type: string
          minLength: 1
          maxLength: 250000
        changeSummary:
          type: string
          maxLength: 4000
        expectedRevision:
          type: integer
          minimum: 1
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [documentId, contentMarkdown, expectedRevision, idempotencyKey]
    DevelopmentDocumentStatusPayload:
      type: object
      additionalProperties: false
      properties:
        documentId:
          type: string
        status:
          type: string
          enum: [draft, ready, approved, superseded, archived]
        expectedRevision:
          type: integer
          minimum: 1
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [documentId, status, expectedRevision, idempotencyKey]
    DevelopmentDocumentMutationResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        document:
          $ref: '#/components/schemas/DevelopmentDocumentRecord'
        currentVersion:
          $ref: '#/components/schemas/DevelopmentDocumentVersionSummary'
        currentContent:
          $ref: '#/components/schemas/DevelopmentDocumentVersionRecord'
        versions:
          type: array
          items:
            $ref: '#/components/schemas/DevelopmentDocumentVersionSummary'
        replayed:
          type: boolean
      required: [ok, document, currentVersion, currentContent, versions, replayed]
    DevelopmentDocumentListResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        documents:
          type: array
          items:
            $ref: '#/components/schemas/DevelopmentDocumentSummary'
      required: [ok, documents]
    DevelopmentDocumentReadResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        document:
          $ref: '#/components/schemas/DevelopmentDocumentRecord'
        currentVersion:
          $ref: '#/components/schemas/DevelopmentDocumentVersionSummary'
        currentContent:
          $ref: '#/components/schemas/DevelopmentDocumentVersionRecord'
        versions:
          type: array
          items:
            $ref: '#/components/schemas/DevelopmentDocumentVersionSummary'
      required: [ok, document, currentVersion, currentContent, versions]
    DevelopmentDocumentVersionReadResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        version:
          $ref: '#/components/schemas/DevelopmentDocumentVersionRecord'
      required: [ok, version]
    TaskDocumentBindPayload:
      type: object
      additionalProperties: false
      properties:
        taskId:
          type: string
        specId:
          oneOf:
            - type: string
            - type: "null"
        planId:
          oneOf:
            - type: string
            - type: "null"
        expectedTaskRevision:
          type: integer
          minimum: 1
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [taskId, specId, planId, expectedTaskRevision, idempotencyKey]
    TaskDocumentBindResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        task:
          $ref: '#/components/schemas/ContinuityTaskRecord'
        spec:
          oneOf:
            - $ref: '#/components/schemas/DevelopmentDocumentSummary'
            - type: "null"
        plan:
          oneOf:
            - $ref: '#/components/schemas/DevelopmentDocumentSummary'
            - type: "null"
        replayed:
          type: boolean
      required: [ok, task, spec, plan, replayed]
    ContinuityTaskCreatePayload:
      type: object
      additionalProperties: false
      properties:
        projectId:
          type: string
        workspaceId:
          type: string
        specId:
          oneOf:
            - type: string
            - type: "null"
        planId:
          oneOf:
            - type: string
            - type: "null"
        parentTaskId:
          oneOf:
            - type: string
            - type: "null"
        title:
          type: string
          minLength: 1
          maxLength: 240
        goal:
          type: string
          minLength: 1
          maxLength: 12000
        priority:
          type: string
          enum: [low, normal, high, critical]
          default: normal
        executionPolicy:
          type: string
          enum: [planning-required, planning-optional]
          default: planning-optional
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [projectId, workspaceId, title, goal, idempotencyKey]
    ContinuityTaskSubmitReviewPayload:
      type: object
      additionalProperties: false
      properties:
        taskId:
          type: string
        expectedRevision:
          type: integer
          minimum: 1
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [taskId, expectedRevision, idempotencyKey]
    ContinuityTaskSubmitReviewResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        task:
          $ref: '#/components/schemas/ContinuityTaskRecord'
        evidenceBundle:
          $ref: '#/components/schemas/ContinuityEvidenceBundleRecord'
        replayed:
          type: boolean
      required: [ok, task, evidenceBundle, replayed]
    ContinuityTaskCompletePayload:
      type: object
      additionalProperties: false
      properties:
        taskId:
          type: string
        expectedRevision:
          type: integer
          minimum: 1
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [taskId, expectedRevision, idempotencyKey]
    ContinuityTaskCompleteResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        task:
          $ref: '#/components/schemas/ContinuityTaskRecord'
        sessions:
          type: array
          items:
            $ref: '#/components/schemas/RuntimeDevelopmentSessionRecord'
        handoff:
          $ref: '#/components/schemas/ContinuityHandoffRecord'
        evidenceBundle:
          $ref: '#/components/schemas/ContinuityEvidenceBundleRecord'
        replayed:
          type: boolean
      required: [ok, task, sessions, handoff, evidenceBundle, replayed]
    ContinuitySessionStartPayload:
      type: object
      additionalProperties: false
      properties:
        taskId:
          type: string
        title:
          type: string
          minLength: 1
          maxLength: 240
        mode:
          type: string
          enum: [chat-direct, codex-session, async-agent]
        expectedTaskRevision:
          type: integer
          minimum: 1
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [taskId, title, mode, expectedTaskRevision, idempotencyKey]
    ContinuityLeaseAcquirePayload:
      type: object
      additionalProperties: false
      properties:
        sessionId:
          type: string
        holderId:
          type: string
        expiresAt:
          type: string
          format: date-time
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [sessionId, holderId, expiresAt, idempotencyKey]
    ContinuityLeaseReleasePayload:
      type: object
      additionalProperties: false
      properties:
        leaseId:
          type: string
        sessionId:
          type: string
        holderId:
          type: string
        expectedRevision:
          type: integer
          minimum: 1
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [leaseId, sessionId, holderId, expectedRevision, idempotencyKey]
    ContinuityHandoffPreparePayload:
      type: object
      additionalProperties: false
      properties:
        taskId:
          type: string
        sessionId:
          type: string
        toMode:
          type: string
          enum: [chat-direct, codex-session, async-agent, unassigned]
          default: unassigned
        goal:
          type: string
          minLength: 1
          maxLength: 12000
        completedItems:
          type: array
          maxItems: 200
          items:
            type: string
        pendingItems:
          type: array
          maxItems: 200
          items:
            type: string
        changedFiles:
          type: array
          maxItems: 500
          items:
            type: string
        risks:
          type: array
          maxItems: 200
          items:
            type: string
        nextAction:
          type: string
          minLength: 1
          maxLength: 4000
        gitHead:
          oneOf:
            - type: string
            - type: "null"
        gitBranch:
          oneOf:
            - type: string
            - type: "null"
        gitDirty:
          type: boolean
        diffArtifactId:
          oneOf:
            - type: string
            - type: "null"
        evidenceBundleId:
          oneOf:
            - type: string
            - type: "null"
        expectedTaskRevision:
          type: integer
          minimum: 1
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [taskId, sessionId, goal, nextAction, gitDirty, expectedTaskRevision, idempotencyKey]
    ContinuityHandoffAcceptPayload:
      type: object
      additionalProperties: false
      properties:
        handoffId:
          type: string
        expectedRevision:
          type: integer
          minimum: 1
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [handoffId, expectedRevision, idempotencyKey]
    ContinuityHandoffCancelPayload:
      $ref: '#/components/schemas/ContinuityHandoffAcceptPayload'
    ContinuityHandoffForkPayload:
      type: object
      additionalProperties: false
      properties:
        handoffId:
          type: string
        expectedRevision:
          type: integer
          minimum: 1
        title:
          type: string
          minLength: 1
          maxLength: 240
        sessionTitle:
          type: string
          minLength: 1
          maxLength: 240
        mode:
          type: string
          enum: [chat-direct, codex-session, async-agent]
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [handoffId, expectedRevision, title, sessionTitle, idempotencyKey]
    ContinuityEvidenceRecordPayload:
      type: object
      additionalProperties: false
      properties:
        taskId:
          type: string
        sessionId:
          type: string
        bundleId:
          type: string
        kind:
          type: string
          enum: [command, test, build, lint, typecheck, diff, review, screenshot, manual]
        label:
          type: string
          minLength: 1
          maxLength: 240
        status:
          type: string
          enum: [passed, failed, skipped, not-run]
        required:
          type: boolean
          default: false
        command:
          oneOf:
            - type: string
            - type: "null"
        exitCode:
          oneOf:
            - type: integer
            - type: "null"
        artifactId:
          oneOf:
            - type: string
            - type: "null"
        summary:
          type: string
          maxLength: 8000
        startedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        completedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        expectedTaskRevision:
          type: integer
          minimum: 1
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [taskId, sessionId, kind, label, status, idempotencyKey]
    ContinuityProjectRecord:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        slug:
          type: string
        displayName:
          type: string
        defaultWorkspaceId:
          oneOf:
            - type: string
            - type: "null"
        status:
          type: string
          enum: [active, archived]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        revision:
          type: integer
          minimum: 1
      required: [id, slug, displayName, defaultWorkspaceId, status, createdAt, updatedAt, revision]
    ContinuityWorkspaceRecord:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        projectId:
          type: string
        repoId:
          type: string
        kind:
          type: string
          enum: [checkout, worktree]
        branch:
          oneOf:
            - type: string
            - type: "null"
        headCommit:
          oneOf:
            - type: string
            - type: "null"
        dirty:
          type: boolean
        status:
          type: string
          enum: [ready, missing, blocked, archived]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        revision:
          type: integer
          minimum: 1
      required: [id, projectId, repoId, kind, branch, headCommit, dirty, status, createdAt, updatedAt, revision]
    ContinuityTaskRecord:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        projectId:
          type: string
        workspaceId:
          type: string
        specId:
          oneOf:
            - type: string
            - type: "null"
        specVersion:
          oneOf:
            - type: integer
              minimum: 1
            - type: "null"
        planId:
          oneOf:
            - type: string
            - type: "null"
        planVersion:
          oneOf:
            - type: integer
              minimum: 1
            - type: "null"
        parentTaskId:
          oneOf:
            - type: string
            - type: "null"
        title:
          type: string
        goal:
          type: string
        status:
          type: string
          enum: [backlog, ready, in-progress, blocked, review, completed, cancelled]
        priority:
          type: string
          enum: [low, normal, high, critical]
        executionPolicy:
          type: string
          enum: [planning-required, planning-optional]
        activeSessionId:
          oneOf:
            - type: string
            - type: "null"
        latestHandoffId:
          oneOf:
            - type: string
            - type: "null"
        latestEvidenceBundleId:
          oneOf:
            - type: string
            - type: "null"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        revision:
          type: integer
          minimum: 1
      required: [id, projectId, workspaceId, specId, specVersion, planId, planVersion, parentTaskId, title, goal, status, priority, executionPolicy, activeSessionId, latestHandoffId, latestEvidenceBundleId, createdAt, updatedAt, revision]
    TaskPlanningRequirementAssessment:
      type: object
      additionalProperties: false
      properties:
        kind:
          type: string
          enum: [spec, plan]
        state:
          type: string
          enum: [not-bound, relation-invalid, unapproved, stale, approved-current]
        documentId:
          oneOf:
            - type: string
            - type: "null"
        pinnedVersion:
          oneOf:
            - type: integer
              minimum: 1
            - type: "null"
        currentVersion:
          oneOf:
            - type: integer
              minimum: 1
            - type: "null"
        status:
          oneOf:
            - type: string
              enum: [draft, ready, approved, superseded, archived]
            - type: "null"
      required: [kind, state, documentId, pinnedVersion, currentVersion, status]
    TaskExecutionPolicyAssessment:
      type: object
      additionalProperties: false
      properties:
        taskId:
          type: string
        policy:
          type: string
          enum: [planning-required, planning-optional]
        allowed:
          type: boolean
        blockers:
          type: array
          items:
            type: string
        spec:
          $ref: '#/components/schemas/TaskPlanningRequirementAssessment'
        plan:
          $ref: '#/components/schemas/TaskPlanningRequirementAssessment'
      required: [taskId, policy, allowed, blockers, spec, plan]
    ContinuityWriterLeaseRecord:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        workspaceId:
          type: string
        sessionId:
          type: string
        holderType:
          type: string
          enum: [chat-direct, codex-session, async-agent]
        holderId:
          type: string
        status:
          type: string
          enum: [active, released, expired, revoked]
        acquiredAt:
          type: string
          format: date-time
        heartbeatAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        releasedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        revision:
          type: integer
          minimum: 1
      required: [id, workspaceId, sessionId, holderType, holderId, status, acquiredAt, heartbeatAt, expiresAt, releasedAt, revision]
    ContinuityHandoffRecord:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        taskId:
          type: string
        sessionId:
          type: string
        workspaceId:
          type: string
        fromMode:
          type: string
          enum: [chat-direct, codex-session, async-agent]
        toMode:
          type: string
          enum: [chat-direct, codex-session, async-agent, unassigned]
        goal:
          type: string
        completedItems:
          type: array
          items:
            type: string
        pendingItems:
          type: array
          items:
            type: string
        changedFiles:
          type: array
          items:
            type: string
        risks:
          type: array
          items:
            type: string
        nextAction:
          type: string
        gitHead:
          oneOf:
            - type: string
            - type: "null"
        gitBranch:
          oneOf:
            - type: string
            - type: "null"
        gitDirty:
          type: boolean
        diffArtifactId:
          oneOf:
            - type: string
            - type: "null"
        evidenceBundleId:
          oneOf:
            - type: string
            - type: "null"
        status:
          type: string
          enum: [draft, ready, accepted, superseded]
        createdAt:
          type: string
          format: date-time
        acceptedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        revision:
          type: integer
          minimum: 1
      required: [id, taskId, sessionId, workspaceId, fromMode, toMode, goal, completedItems, pendingItems, changedFiles, risks, nextAction, gitHead, gitBranch, gitDirty, diffArtifactId, evidenceBundleId, status, createdAt, acceptedAt, revision]
    ContinuityEvidenceBundleRecord:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        taskId:
          type: string
        sessionId:
          type: string
        status:
          type: string
          enum: [collecting, complete, incomplete]
        requiredItemCount:
          type: integer
          minimum: 0
        passedItemCount:
          type: integer
          minimum: 0
        failedItemCount:
          type: integer
          minimum: 0
        skippedItemCount:
          type: integer
          minimum: 0
        createdAt:
          type: string
          format: date-time
        completedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        revision:
          type: integer
          minimum: 1
      required: [id, taskId, sessionId, status, requiredItemCount, passedItemCount, failedItemCount, skippedItemCount, createdAt, completedAt, revision]
    ContinuityEvidenceItemRecord:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        bundleId:
          type: string
        kind:
          type: string
          enum: [command, test, build, lint, typecheck, diff, review, screenshot, manual]
        label:
          type: string
        status:
          type: string
          enum: [passed, failed, skipped, not-run]
        required:
          type: boolean
        command:
          oneOf:
            - type: string
            - type: "null"
        exitCode:
          oneOf:
            - type: integer
            - type: "null"
        artifactId:
          oneOf:
            - type: string
            - type: "null"
        summary:
          type: string
        startedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        completedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        createdAt:
          type: string
          format: date-time
      required: [id, bundleId, kind, label, status, required, command, exitCode, artifactId, summary, startedAt, completedAt, createdAt]
    WorkspaceEvidenceProjection:
      type: object
      additionalProperties: false
      properties:
        bundle:
          $ref: '#/components/schemas/ContinuityEvidenceBundleRecord'
        items:
          type: array
          items:
            $ref: '#/components/schemas/ContinuityEvidenceItemRecord'
        verificationState:
          type: string
          enum: [verified, incomplete, missing]
      required: [bundle, items, verificationState]
    TaskCompletionBlocker:
      type: object
      additionalProperties: false
      properties:
        code:
          type: string
        message:
          type: string
        details:
          type: object
          additionalProperties: true
      required: [code, message]
    TaskCompletionAssessmentProjection:
      type: object
      additionalProperties: false
      properties:
        eligible:
          type: boolean
        blockers:
          type: array
          items:
            $ref: '#/components/schemas/TaskCompletionBlocker'
      required: [eligible, blockers]
    WorkspaceRuntimeJobProjection:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        status:
          type: string
          enum: [queued, running, completed, failed]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        artifacts:
          type: array
          items:
            $ref: '#/components/schemas/JobArtifactSummary'
      required: [id, status, createdAt, updatedAt, artifacts]
    WorkspaceSessionRuntimeProjection:
      type: object
      additionalProperties: false
      properties:
        sessionId:
          type: string
        binding:
          oneOf:
            - $ref: '#/components/schemas/RuntimeBindingRecord'
            - $ref: '#/components/schemas/RunnerRuntimeBindingRecord'
            - type: "null"
        job:
          oneOf:
            - $ref: '#/components/schemas/WorkspaceRuntimeJobProjection'
            - type: "null"
      required: [sessionId, binding, job]
    WorkspaceTaskContinuityProjection:
      type: object
      additionalProperties: false
      properties:
        task:
          $ref: '#/components/schemas/ContinuityTaskRecord'
        sessions:
          type: array
          items:
            $ref: '#/components/schemas/RuntimeDevelopmentSessionRecord'
        runtimes:
          type: array
          items:
            $ref: '#/components/schemas/WorkspaceSessionRuntimeProjection'
        latestHandoff:
          oneOf:
            - $ref: '#/components/schemas/ContinuityHandoffRecord'
            - type: "null"
        evidence:
          oneOf:
            - $ref: '#/components/schemas/WorkspaceEvidenceProjection'
            - type: "null"
        executionPolicy:
          $ref: '#/components/schemas/TaskExecutionPolicyAssessment'
        completion:
          $ref: '#/components/schemas/TaskCompletionAssessmentProjection'
      required: [task, sessions, runtimes, latestHandoff, evidence, executionPolicy, completion]
    WorkspaceGitProjection:
      type: object
      additionalProperties: false
      properties:
        available:
          type: boolean
        branch:
          oneOf:
            - type: string
            - type: "null"
        headCommit:
          oneOf:
            - type: string
            - type: "null"
        dirty:
          type: boolean
        changedPaths:
          type: array
          items:
            type: string
        unavailableReason:
          oneOf:
            - type: string
            - type: "null"
      required: [available, branch, headCommit, dirty, changedPaths, unavailableReason]
    WorkspaceContinuitySnapshot:
      type: object
      additionalProperties: false
      properties:
        project:
          $ref: '#/components/schemas/ContinuityProjectRecord'
        workspace:
          $ref: '#/components/schemas/ContinuityWorkspaceRecord'
        activeLease:
          oneOf:
            - $ref: '#/components/schemas/ContinuityWriterLeaseRecord'
            - type: "null"
        readOnly:
          type: boolean
        readOnlyReason:
          oneOf:
            - type: string
              enum: [active-writer]
            - type: "null"
        git:
          $ref: '#/components/schemas/WorkspaceGitProjection'
        tasks:
          type: array
          items:
            $ref: '#/components/schemas/WorkspaceTaskContinuityProjection'
        pendingApprovals:
          type: array
          items:
            $ref: '#/components/schemas/RuntimeApprovalRecord'
      required: [project, workspace, activeLease, readOnly, readOnlyReason, git, tasks, pendingApprovals]
    WorkspaceContinuitySnapshotResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        snapshot:
          $ref: '#/components/schemas/WorkspaceContinuitySnapshot'
      required: [ok, snapshot]
    CodexStandaloneOperationCapability:
      type: object
      additionalProperties: false
      properties:
        operation:
          type: string
        method:
          oneOf:
            - type: string
            - type: "null"
        status:
          type: string
          enum: [verified, unavailable, failed]
        safeForChatDirect:
          type: boolean
        errorCode:
          oneOf:
            - type: string
            - type: "null"
        evidence:
          type: object
          additionalProperties:
            oneOf:
              - type: boolean
              - type: number
              - type: string
              - type: "null"
      required: [operation, method, status, safeForChatDirect, errorCode, evidence]
    CodexStandaloneCapabilitySnapshot:
      type: object
      additionalProperties: false
      description: Verified standalone App Server capabilities. Probe paths and file contents are omitted.
      properties:
        schemaVersion:
          type: integer
          const: 1
        runtime:
          type: string
          const: codex-app-server
        protocolFamily:
          type: string
          const: app-server-v2
        binarySource:
          oneOf:
            - type: string
            - type: "null"
        binaryVersion:
          oneOf:
            - type: string
            - type: "null"
        serverProtocolVersion:
          oneOf:
            - type: string
            - type: "null"
        probedAt:
          type: string
          format: date-time
        operations:
          type: object
          additionalProperties:
            $ref: '#/components/schemas/CodexStandaloneOperationCapability'
        outgoingMethods:
          type: array
          items:
            type: string
        turnStartObserved:
          type: boolean
        directExecutionReady:
          type: boolean
      required:
        - schemaVersion
        - runtime
        - protocolFamily
        - binarySource
        - binaryVersion
        - serverProtocolVersion
        - probedAt
        - operations
        - outgoingMethods
        - turnStartObserved
        - directExecutionReady
    RuntimeCapabilitySnapshot:
      type: object
      additionalProperties: false
      properties:
        available:
          type: boolean
        runtime:
          type: string
          const: codex-app-server
        binarySource:
          oneOf:
            - type: string
            - type: "null"
        binaryVersion:
          oneOf:
            - type: string
            - type: "null"
        protocolFamily:
          type: string
          const: app-server-v2
        serverProtocolVersion:
          oneOf:
            - type: string
            - type: "null"
        stableMethods:
          type: array
          items:
            type: string
        experimentalApiEnabled:
          type: boolean
        standaloneExecution:
          oneOf:
            - $ref: '#/components/schemas/CodexStandaloneCapabilitySnapshot'
            - type: "null"
        unavailableReason:
          type: string
      required:
        - available
        - runtime
        - binarySource
        - binaryVersion
        - protocolFamily
        - serverProtocolVersion
        - stableMethods
        - experimentalApiEnabled
        - standaloneExecution
    RuntimeThreadStatus:
      type: object
      additionalProperties: false
      properties:
        type:
          type: string
        activeFlags:
          type: array
          items:
            type: string
      required: [type]
    RuntimeThreadProjection:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        preview:
          type: string
        modelProvider:
          oneOf:
            - type: string
            - type: "null"
        createdAt:
          oneOf:
            - type: number
            - type: "null"
        updatedAt:
          oneOf:
            - type: number
            - type: "null"
        recencyAt:
          oneOf:
            - type: number
            - type: "null"
        sourceKind:
          oneOf:
            - type: string
            - type: "null"
        status:
          $ref: '#/components/schemas/RuntimeThreadStatus'
        projectId:
          oneOf:
            - type: string
            - type: "null"
        workspaceId:
          oneOf:
            - type: string
            - type: "null"
        repoId:
          oneOf:
            - type: string
            - type: "null"
        parentThreadId:
          oneOf:
            - type: string
            - type: "null"
        agentNickname:
          oneOf:
            - type: string
            - type: "null"
        agentRole:
          oneOf:
            - type: string
            - type: "null"
      required:
        - id
        - preview
        - modelProvider
        - createdAt
        - updatedAt
        - recencyAt
        - sourceKind
        - status
        - projectId
        - workspaceId
        - repoId
        - parentThreadId
        - agentNickname
        - agentRole
    RuntimeCapabilitiesResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        capabilities:
          $ref: '#/components/schemas/RuntimeCapabilitySnapshot'
      required: [ok, capabilities]
    RuntimeThreadListResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        data:
          type: array
          items:
            $ref: '#/components/schemas/RuntimeThreadProjection'
        nextCursor:
          oneOf:
            - type: string
            - type: "null"
        backwardsCursor:
          oneOf:
            - type: string
            - type: "null"
      required: [ok, data, nextCursor, backwardsCursor]
    RuntimeThreadReadResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        thread:
          $ref: '#/components/schemas/RuntimeThreadProjection'
      required: [ok, thread]
    RuntimeSessionMutationPayload:
      type: object
      additionalProperties: false
      properties:
        sessionId:
          type: string
          minLength: 1
          maxLength: 160
        threadId:
          type: string
          minLength: 1
          maxLength: 240
        expectedSessionRevision:
          type: integer
          minimum: 1
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [sessionId, threadId, expectedSessionRevision, idempotencyKey]
    RuntimeSessionForkPayload:
      allOf:
        - $ref: '#/components/schemas/RuntimeSessionMutationPayload'
        - type: object
          additionalProperties: false
          properties:
            lastTurnId:
              oneOf:
                - type: string
                  minLength: 1
                  maxLength: 240
                - type: "null"
    AsyncJobQueuePayload:
      type: object
      additionalProperties: false
      properties:
        taskId: { type: string }
        sessionId: { type: string }
        expectedTaskRevision: { type: integer, minimum: 1 }
        expectedSessionRevision: { type: integer, minimum: 1 }
        repoId: { type: string }
        title: { type: string }
        instructions:
          type: string
          description: Private execution instructions persisted only in the local Job record and omitted from the queue response.
        executionMode:
          type: string
          enum: [plan, review, develop]
          default: develop
        worktreePolicy:
          type: string
          enum: [auto, always, never]
          default: auto
        branchName: { type: string }
        approvalPolicy:
          type: string
          enum: [untrusted, on-request, never]
          default: never
        sandbox:
          type: string
          enum: [read-only, workspace-write, danger-full-access]
          default: workspace-write
        verificationCommands:
          type: array
          items: { type: string }
        acceptanceCriteria:
          type: array
          items: { type: string }
        commitPolicy:
          type: string
          enum: [none, propose, commit]
          default: propose
        commitTitle: { type: string }
        commitBody: { type: string }
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required:
        [taskId, sessionId, expectedTaskRevision, expectedSessionRevision, repoId, title, instructions, idempotencyKey]
    AsyncJobPublicRecord:
      type: object
      additionalProperties: false
      properties:
        id: { type: string }
        type:
          type: string
          const: codex-run
        status:
          type: string
          enum: [queued, running, completed, failed]
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        payload:
          type: object
          additionalProperties: false
          properties:
            repoId: { type: string }
            title: { type: string }
            executionMode:
              type: string
              enum: [plan, review, develop]
            worktreePolicy:
              type: string
              enum: [auto, always, never]
            commitPolicy:
              type: string
              enum: [none, propose, commit]
            continuityTaskId: { type: string }
            continuitySessionId: { type: string }
            continuityRuntimeBindingId: { type: string }
          required:
            [repoId, title, executionMode, worktreePolicy, commitPolicy, continuityTaskId, continuitySessionId, continuityRuntimeBindingId]
      required: [id, type, status, createdAt, updatedAt, payload]
    RunnerRuntimeBindingRecord:
      type: object
      additionalProperties: false
      properties:
        id: { type: string }
        sessionId: { type: string }
        workspaceId: { type: string }
        runtimeKind:
          type: string
          const: tokenpilot-runner
        externalSessionId:
          type: "null"
        externalRunId: { type: string }
        sourceExternalId:
          oneOf:
            - type: string
            - type: "null"
        externalThreadId:
          type: "null"
        sourceThreadId:
          type: "null"
        relation:
          type: string
          const: queued
        status:
          type: string
          enum: [active, superseded, released, stale]
        modelProvider:
          type: "null"
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        revision: { type: integer, minimum: 1 }
      required:
        [id, sessionId, workspaceId, runtimeKind, externalSessionId, externalRunId, sourceExternalId, externalThreadId, sourceThreadId, relation, status, modelProvider, createdAt, updatedAt, revision]
    AsyncJobQueueResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        task:
          $ref: '#/components/schemas/ContinuityTaskRecord'
        session:
          $ref: '#/components/schemas/RuntimeDevelopmentSessionRecord'
        binding:
          $ref: '#/components/schemas/RunnerRuntimeBindingRecord'
        job:
          $ref: '#/components/schemas/AsyncJobPublicRecord'
        executionPolicy:
          $ref: '#/components/schemas/TaskExecutionPolicyAssessment'
        replayed: { type: boolean }
      required: [ok, task, session, binding, job, executionPolicy, replayed]
    RuntimeBindingRecord:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        sessionId:
          type: string
        workspaceId:
          type: string
        runtimeKind:
          type: string
          const: codex-app-server
        externalThreadId:
          type: string
        sourceThreadId:
          oneOf:
            - type: string
            - type: "null"
        relation:
          type: string
          enum: [bound, resumed, forked]
        status:
          type: string
          enum: [active, superseded, released, stale]
        modelProvider:
          oneOf:
            - type: string
            - type: "null"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        revision:
          type: integer
          minimum: 1
      required:
        - id
        - sessionId
        - workspaceId
        - runtimeKind
        - externalThreadId
        - sourceThreadId
        - relation
        - status
        - modelProvider
        - createdAt
        - updatedAt
        - revision
    RuntimeDevelopmentSessionRecord:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        projectId:
          type: string
        workspaceId:
          type: string
        taskId:
          type: string
        title:
          type: string
        mode:
          type: string
          enum: [chat-direct, codex-session, async-agent]
        status:
          type: string
          enum: [idle, running, waiting-approval, handoff-ready, completed, failed]
        activeRuntimeBindingId:
          oneOf:
            - type: string
            - type: "null"
        startedAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        endedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        revision:
          type: integer
          minimum: 1
      required:
        - id
        - projectId
        - workspaceId
        - taskId
        - title
        - mode
        - status
        - activeRuntimeBindingId
        - startedAt
        - updatedAt
        - endedAt
        - revision
    RuntimeBindingMutationResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        binding:
          $ref: '#/components/schemas/RuntimeBindingRecord'
        session:
          $ref: '#/components/schemas/RuntimeDevelopmentSessionRecord'
        thread:
          $ref: '#/components/schemas/RuntimeThreadProjection'
        replayed:
          type: boolean
      required: [ok, binding, session, thread, replayed]
    RuntimeTurnStartPayload:
      type: object
      additionalProperties: false
      properties:
        sessionId:
          type: string
          minLength: 1
          maxLength: 160
        text:
          type: string
          minLength: 1
          maxLength: 50000
        expectedSessionRevision:
          type: integer
          minimum: 1
        expectedTaskRevision:
          type: integer
          minimum: 1
        leaseDurationSeconds:
          type: integer
          minimum: 60
          maximum: 3600
          default: 900
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required:
        - sessionId
        - text
        - expectedSessionRevision
        - expectedTaskRevision
        - idempotencyKey
    RuntimeTurnInterruptPayload:
      type: object
      additionalProperties: false
      properties:
        runId:
          type: string
          minLength: 1
          maxLength: 160
        expectedRunRevision:
          type: integer
          minimum: 1
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [runId, expectedRunRevision, idempotencyKey]
    RuntimeApprovalRespondPayload:
      type: object
      additionalProperties: false
      properties:
        approvalId:
          type: string
          minLength: 1
          maxLength: 160
        expectedRevision:
          type: integer
          minimum: 1
        decision:
          type: string
          enum: [accept, decline, cancel]
        idempotencyKey:
          type: string
          minLength: 8
          maxLength: 128
          pattern: '^[A-Za-z0-9._:-]+$'
      required: [approvalId, expectedRevision, decision, idempotencyKey]
    RuntimeTurnProjection:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        status:
          type: string
        startedAt:
          oneOf:
            - type: number
            - type: "null"
        completedAt:
          oneOf:
            - type: number
            - type: "null"
        durationMs:
          oneOf:
            - type: number
            - type: "null"
        errorCode:
          oneOf:
            - type: string
            - type: "null"
      required: [id, status, startedAt, completedAt, durationMs, errorCode]
    RuntimeRunRecord:
      type: object
      additionalProperties: false
      properties:
        id:
          type: string
        sessionId:
          type: string
        workspaceId:
          type: string
        runtimeBindingId:
          type: string
        threadId:
          type: string
        externalTurnId:
          oneOf:
            - type: string
            - type: "null"
        status:
          type: string
          enum: [starting, running, waiting-approval, completed, failed, interrupted, stale]
        inputHash:
          type: string
          description: SHA-256 hash of the submitted input. The raw input is not exposed here.
        inputLength:
          type: integer
          minimum: 0
        handoffId:
          type: string
        evidenceBundleId:
          type: string
        writerLeaseId:
          type: string
        modelLoopOwner:
          type: string
          const: codex
        approvalPolicy:
          type: string
          const: on-request
        startedAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        completedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        errorCode:
          oneOf:
            - type: string
            - type: "null"
        revision:
          type: integer
          minimum: 1
      required:
        - id
        - sessionId
        - workspaceId
        - runtimeBindingId
        - threadId
        - externalTurnId
        - status
        - inputHash
        - inputLength
        - handoffId
        - evidenceBundleId
        - writerLeaseId
        - modelLoopOwner
        - approvalPolicy
        - startedAt
        - updatedAt
        - completedAt
        - errorCode
        - revision
    RuntimeApprovalRecord:
      type: object
      additionalProperties: false
      description: Public approval projection. Internal App Server request handles and raw private request parameters are omitted.
      properties:
        id:
          type: string
        runId:
          type: string
        sessionId:
          type: string
        workspaceId:
          type: string
        threadId:
          type: string
        turnId:
          type: string
        itemId:
          oneOf:
            - type: string
            - type: "null"
        requestMethod:
          type: string
        kind:
          type: string
          enum: [command-execution, file-change, permissions, unsupported]
        status:
          type: string
          enum: [pending, responded, resolved, cancelled, stale]
        publicSummary:
          type: object
          additionalProperties: true
        decision:
          oneOf:
            - type: object
              additionalProperties: true
            - type: "null"
        receivedAt:
          type: string
          format: date-time
        respondedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        resolvedAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        revision:
          type: integer
          minimum: 1
      required:
        - id
        - runId
        - sessionId
        - workspaceId
        - threadId
        - turnId
        - itemId
        - requestMethod
        - kind
        - status
        - publicSummary
        - decision
        - receivedAt
        - respondedAt
        - resolvedAt
        - revision
    RuntimeEventRecord:
      type: object
      additionalProperties: false
      description: Append-only public-safe runtime event. Raw prompts, cwd, patches, command output, and private approval payloads are omitted.
      properties:
        sequence:
          type: integer
          minimum: 1
        id:
          type: string
        runId:
          oneOf:
            - type: string
            - type: "null"
        sessionId:
          type: string
        workspaceId:
          type: string
        threadId:
          type: string
        turnId:
          oneOf:
            - type: string
            - type: "null"
        itemId:
          oneOf:
            - type: string
            - type: "null"
        method:
          type: string
        category:
          type: string
          enum: [lifecycle, approval, item, warning, error, other]
        publicPayload:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
      required:
        - sequence
        - id
        - runId
        - sessionId
        - workspaceId
        - threadId
        - turnId
        - itemId
        - method
        - category
        - publicPayload
        - createdAt
    RuntimeTurnStartResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        run:
          $ref: '#/components/schemas/RuntimeRunRecord'
        lease:
          type: object
          additionalProperties: true
        handoff:
          type: object
          additionalProperties: true
        evidenceBundle:
          type: object
          additionalProperties: true
        session:
          $ref: '#/components/schemas/RuntimeDevelopmentSessionRecord'
        task:
          type: object
          additionalProperties: true
        executionPolicy:
          $ref: '#/components/schemas/TaskExecutionPolicyAssessment'
        turn:
          $ref: '#/components/schemas/RuntimeTurnProjection'
        replayed:
          type: boolean
      required: [ok, run, lease, handoff, evidenceBundle, session, task, executionPolicy, turn, replayed]
    RuntimeTurnInterruptResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        run:
          $ref: '#/components/schemas/RuntimeRunRecord'
        session:
          $ref: '#/components/schemas/RuntimeDevelopmentSessionRecord'
        lease:
          type: object
          additionalProperties: true
        replayed:
          type: boolean
      required: [ok, run, session, lease, replayed]
    RuntimeApprovalRespondResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        approval:
          $ref: '#/components/schemas/RuntimeApprovalRecord'
        run:
          $ref: '#/components/schemas/RuntimeRunRecord'
        session:
          $ref: '#/components/schemas/RuntimeDevelopmentSessionRecord'
        replayed:
          type: boolean
      required: [ok, approval, run, session, replayed]
    RuntimeEventsResponse:
      type: object
      additionalProperties: false
      properties:
        ok:
          type: boolean
          const: true
        events:
          type: array
          items:
            $ref: '#/components/schemas/RuntimeEventRecord'
        nextSequence:
          oneOf:
            - type: integer
              minimum: 1
            - type: "null"
      required: [ok, events, nextSequence]
paths:
  /api/health:
    get:
      operationId: health
      summary: Health check
      description: Returns the current TokenPilot control-plane health information.
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /api/setup/status:
    get:
      operationId: getSetupStatus
      summary: Get first-run setup status
      description: Returns public-safe first-run setup readiness without exposing bearer tokens, local filesystem paths, or private deployment details.
      responses:
        "200":
          description: Setup status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SetupStatusResponse'
  /api/gpt/config:
    get:
      operationId: getGptConfig
      summary: Get the current GPT configuration recommendation
      description: Returns the current recommended GPT configuration, including version, update time, OpenAPI URL, schema import URL, and instructions text.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: GPT config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GptConfigResponse'
  /api/git/recent-commits:
    get:
      operationId: getRecentCommits
      summary: Get recent git commit summaries
      description: Returns recent commit metadata so GPT and operators can inspect the latest repository changes without reading the full git history.
      security:
        - bearerAuth: []
      parameters:
        - name: repoId
          in: query
          required: false
          schema:
            type: string
          description: Public repository alias. Defaults to tokenpilot.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
        - name: executorId
          in: query
          required: false
          description: Optional explicit Direct Drive executor id. When set, TokenPilot will not silently fall back to another executor.
          schema:
            type: string
      responses:
        "200":
          description: Recent commits
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecentCommitsResponse'
  /api/jobs/pack:
    post:
      operationId: createPack
      summary: Queue a repo bundle job
      description: Creates a queued pack job that produces a repomix bundle and bundle manifest for an approved repo alias. If repoId is omitted, the current default repo tokenpilot is used.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PackJobPayload'
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Job accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreatePackResponse'
  /api/jobs/taskpack:
    post:
      operationId: createTaskPack
      summary: Queue a Codex task pack job
      description: Creates a queued task-pack job from a structured problem description.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaskPackJobPayload'
      responses:
        "200":
          description: Job accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateTaskPackResponse'
  /api/jobs/codex-run:
    post:
      operationId: createCodexRun
      summary: Queue a local Codex CLI execution job
      description: Creates a queued write-side job that the local runner executes through Codex CLI in an allowlisted repo. This is not a raw shell endpoint.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CodexRunJobPayload'
      responses:
        "200":
          description: Job accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateCodexRunResponse'
  /api/jobs:
    get:
      operationId: listJobs
      summary: List queued and completed jobs
      description: Returns a bounded job summary page from the local TokenPilot queue. Results are omitted by default; call getJob for detail or set includeResult=true for explicit operator inspection.
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: cursor
          in: query
          required: false
          schema:
            type: string
          description: Offset cursor returned by the previous listJobs response.
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [queued, running, completed, failed]
        - name: type
          in: query
          required: false
          schema:
            type: string
            enum: [pack, taskpack, codex-run]
        - name: includeResult
          in: query
          required: false
          schema:
            type: string
            enum: ["true", "false", "1", "0"]
          description: Defaults to false for compact GPT Actions responses.
      responses:
        "200":
          description: Jobs list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobsListResponse'
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "401":
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/jobs/{id}:
    get:
      operationId: getJob
      summary: Get a single job status
      description: Returns a single queued, running, completed, or failed job by id.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Job record
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetJobResponse'
        "404":
          description: Job not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/jobs/{id}/artifacts:
    get:
      operationId: listJobArtifacts
      summary: List public-safe artifacts for a job
      description: Returns the artifact entries declared by the selected job result.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Artifact list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobArtifactsListResponse'
        "404":
          description: Job not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/jobs/{id}/artifacts/{artifactKey}:
    get:
      operationId: readJobArtifact
      summary: Read a public-safe artifact by key
      description: Reads a single text-like artifact that was declared by a completed job result.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: artifactKey
          in: path
          required: true
          schema:
            type: string
            enum: [repomixXml, prompt, summary, manifest, markdown, json, codexPrompt, codexStdout, codexStderr, codexDiff, codexReview, codexSummary]
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
      responses:
        "200":
          description: Artifact content
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobArtifactReadResponse'
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "404":
          description: Artifact or job not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/jobs/{id}/control/{action}:
    post:
      operationId: controlJob
      summary: Pause, resume, or terminate a tracked local job process
      description: Sends process-control signals to the local runner's tracked Codex process. Supported actions are pause, resume, and terminate.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: action
          in: path
          required: true
          schema:
            type: string
            enum: [pause, resume, terminate]
      responses:
        "200":
          description: Control result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobControlResponse'
  /api/jobs/control/terminate-all:
    post:
      operationId: terminateAllJobs
      summary: Terminate all tracked local Codex job processes
      description: Emergency stop for TokenPilot-tracked Codex job processes. This is not a raw shell endpoint.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Termination summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TerminateAllJobsResponse'
  /api/host/roots:
    get:
      operationId: listHostDirectRoots
      summary: List public-safe Host Direct root aliases
      description: Lists local Host Direct root aliases and read access without exposing local absolute paths.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Host Direct root aliases
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HostDirectRootsResponse'
  /api/host/files/read:
    post:
      operationId: readHostDirectFile
      summary: Read a small text-like file from an allowlisted Host Direct root
      description: Reads one text-like file up to 64 KiB through a configured Host Root Alias and a verified Host Direct executor. Absolute paths, traversal, sensitive hidden paths, and symlink escapes are rejected before downstream execution.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HostFileReadPayload'
      responses:
        "200":
          description: Host file content using a public-safe alias path
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HostFileReadResponse'
        "403":
          description: Host path or root access is blocked
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "404":
          description: Host root or file not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "413":
          description: Host file exceeds the current read-only size limit
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/host/mutations/prepare:
    post:
      operationId: prepareHostDirectMutation
      summary: Prepare one governed Host text-file write or exact edit
      description: Validates Host Root write access, path safety, Workspace re-entry, Writer Lease and a verified Host executor, then creates a short-lived exact-mutation approval. No file is modified by this step.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HostMutationPreparePayload'
      responses:
        "200":
          description: Pending Direct Mutation approval
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HostMutationApprovalResponse'
        "403":
          description: Host Root or path policy blocks mutation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "409":
          description: Workspace governance, revision or idempotency conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "413":
          description: Mutation content exceeds the Host text-file size limit
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/host/mutations/decision:
    post:
      operationId: decideHostDirectMutation
      summary: Approve or deny one pending Host mutation
      description: Applies an optimistic-revision approval decision without contacting the downstream executor.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HostMutationDecisionPayload'
      responses:
        "200":
          description: Updated Direct Mutation approval
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HostMutationApprovalResponse'
        "409":
          description: Approval expired, already decided, or revision/idempotency conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/host/mutations/execute:
    post:
      operationId: executeHostDirectMutation
      summary: Execute one approved Host text-file write or exact edit
      description: Revalidates the full approved mutation, current file hash, Workspace governance and verified executor mapping, consumes the approval at-most-once, executes through the normalized downstream capability, then verifies the resulting file hash and records Task Evidence or Pure Host Audit.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HostMutationExecutePayload'
      responses:
        "200":
          description: Verified Host mutation result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HostMutationExecuteResponse'
        "403":
          description: Host Root or path policy blocks mutation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "409":
          description: Approval, hash, Workspace governance, revision or idempotency conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "413":
          description: Mutation content exceeds the Host text-file size limit
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/host/commands/prepare:
    post:
      operationId: prepareHostDirectCommand
      summary: Prepare one governed bounded Host command
      description: Resolves the Host workdir, applies command/effect policy, re-enters Workspace Writer Lease governance for write effects, verifies Desktop Commander process capabilities, and creates a short-lived exact-command approval without starting a process.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HostCommandPreparePayload'
      responses:
        "200":
          description: Pending Direct Command approval
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HostCommandApprovalResponse'
        "403":
          description: Host Root, path, command policy, or effect policy blocks execution
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "409":
          description: Workspace governance, revision, executor, or idempotency conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/host/commands/decision:
    post:
      operationId: decideHostDirectCommand
      summary: Approve or deny one pending Host command
      description: Applies an optimistic-revision decision without starting a process.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HostCommandDecisionPayload'
      responses:
        "200":
          description: Updated Direct Command approval
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HostCommandApprovalResponse'
        "409":
          description: Approval expired, already decided, consumed, or revision/idempotency conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/host/commands/execute:
    post:
      operationId: executeHostDirectCommand
      summary: Execute one approved bounded non-interactive Host command
      description: Revalidates the approved command and governance, consumes approval at-most-once, runs the private bounded Desktop Commander process lifecycle, terminates non-terminal commands, bounds public output, and records Audit or Task Evidence without exposing PID or private cwd.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HostCommandExecutePayload'
      responses:
        "200":
          description: Host command result, including honest non-zero or timeout outcomes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HostCommandExecuteResponse'
        "403":
          description: Host Root, path, command policy, or effect policy blocks execution
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "409":
          description: Approval, hash, Workspace governance, revision, executor, or idempotency conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "502":
          description: Downstream process lifecycle is invalid, unavailable, or has an unknown terminal result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "504":
          description: Host command exceeded the bounded execution timeout
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/host/processes/prepare:
    post:
      operationId: prepareHostManagedProcess
      summary: Prepare one Managed Workspace Process action
      description: Validates one exact start, input, or stop action and creates a short-lived Direct Process approval. Start/input remain Workspace-governed; stop stays available to the owning Session for cleanup. No external process action occurs here.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HostProcessPreparePayload'
      responses:
        "200":
          description: Pending Direct Process approval
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HostProcessApprovalResponse'
        "400":
          description: Invalid structured command or transient process input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "403":
          description: Process scope, policy, or ownership is not allowed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "409":
          description: Workspace, Writer Lease, quota, approval, or idempotency conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/host/processes/decision:
    post:
      operationId: decideHostManagedProcess
      summary: Approve or deny one Managed Process action
      description: Applies an optimistic-revision decision to a short-lived Direct Process approval without invoking Desktop Commander.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HostProcessDecisionPayload'
      responses:
        "200":
          description: Updated Direct Process approval
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HostProcessApprovalResponse'
        "409":
          description: Approval expired, already decided, consumed, or revision/idempotency conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/host/processes/execute:
    post:
      operationId: executeHostManagedProcess
      summary: Execute one approved Managed Process action
      description: Executes an approved start, input, or stop through a TokenPilot-owned process identity. Downstream PID/session handles remain private, mutation results exclude stdout, and Audit/Evidence record only public-safe metadata.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HostProcessExecutePayload'
      responses:
        "200":
          description: Managed Process action result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HostProcessActionResponse'
        "403":
          description: Process scope, policy, or ownership is not allowed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "409":
          description: Process state, Writer Lease, approval, hash, revision, or idempotency conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "502":
          description: Managed downstream process action failed or has an unknown terminal result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "503":
          description: Verified Desktop Commander managed-process dependencies are unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/host/processes/read:
    post:
      operationId: readHostManagedProcess
      summary: Read bounded Managed Process output
      description: Reads public-safe output for one TokenPilot processId, reconciles Writer Lease/runtime ownership, and updates terminal state when observed. OS PID is never accepted or returned.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HostProcessReadPayload'
      responses:
        "200":
          description: Managed Process output and current public state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HostProcessReadResponse'
        "404":
          description: Managed Process not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        "409":
          description: Managed Process is stale or not readable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/host/processes:
    get:
      operationId: listHostManagedProcesses
      summary: List TokenPilot Managed Process records
      description: Lists public-safe TokenPilot process identities and state after runtime/Writer Lease reconciliation. Raw downstream sessions and OS PIDs are never projected.
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: workspaceId
          schema:
            type: string
        - in: query
          name: sessionId
          schema:
            type: string
        - in: query
          name: status
          schema:
            type: string
            enum: [running, exited, terminated, failed, stale]
      responses:
        "200":
          description: Public-safe Managed Process records
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HostProcessListResponse'
  /api/files/read:
    post:
      operationId: readFile
      summary: Read a single allowlisted text file
      description: Reads a single relative text file from an allowlisted repository mapping.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FileReadPayload'
      responses:
        "200":
          description: File content
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReadFileResponse'
        "400":
          description: Validation or path safety error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/files/read-batch:
    post:
      operationId: readFiles
      summary: Read multiple allowlisted text files
      description: Reads up to 10 relative text files from an allowlisted repository mapping.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FileReadBatchPayload'
      responses:
        "200":
          description: File contents
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReadFilesResponse'
        "400":
          description: Validation or path safety error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/files/write:
    post:
      operationId: writeFile
      summary: Write or overwrite a text file
      description: Creates or overwrites a single text file in an allowlisted repository. Content size limited to 512 KB.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FileWritePayload'
      responses:
        "200":
          description: File written
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FileWriteResponse'
  /api/files/edit:
    post:
      operationId: editFile
      summary: Search-and-replace edit in a text file
      description: Finds a unique occurrence of search text and replaces it. Search text must appear exactly once in the file.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FileEditPayload'
      responses:
        "200":
          description: Edit applied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FileEditResponse'
  /api/files/list:
    post:
      operationId: listDirectory
      summary: List directory contents
      description: Lists files and subdirectories in a directory within an allowlisted repository. Hidden files are excluded except common config files.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FileListPayload'
      responses:
        "200":
          description: Directory listing
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FileListResponse'
  /api/search:
    post:
      operationId: searchCode
      summary: Search code across the repository
      description: Runs ripgrep (or grep fallback) across an allowlisted repository. Returns up to 40 matches with optional context lines.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SearchPayload'
      responses:
        "200":
          description: Search results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
  /api/shell/run:
    post:
      operationId: runShell
      summary: Run a whitelisted shell command
      description: Runs an allowlisted command in an approved repo. Output is capped at 64 KB and 25 seconds. High-trust commands such as node, python, npx, tsx, and make are blocked in exposed mode unless explicitly enabled.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ShellRunPayload'
      responses:
        "200":
          description: Command output
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ShellRunResponse'
  /api/git/diff:
    get:
      operationId: getGitDiff
      summary: Get the current git diff
      description: Returns the working tree diff (unstaged changes by default). Public-safe paths only; env files, TokenPilot runtime state, logs, and local agent state are omitted.
      security:
        - bearerAuth: []
      parameters:
        - name: repoId
          in: query
          required: false
          schema:
            type: string
            default: tokenpilot
        - name: staged
          in: query
          required: false
          schema:
            type: string
            enum: ["true", "false", "1", "0"]
        - name: executorId
          in: query
          required: false
          description: Optional explicit Direct Drive executor id. When set, TokenPilot will not silently fall back to another executor.
          schema:
            type: string
      responses:
        "200":
          description: Git diff
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GitDiffResponse'
  /api/git/status:
    get:
      operationId: getGitStatus
      summary: Get the current git status
      description: Returns the working tree status including branch name and per-file status entries. Public-safe paths only.
      security:
        - bearerAuth: []
      parameters:
        - name: repoId
          in: query
          required: false
          schema:
            type: string
            default: tokenpilot
        - name: executorId
          in: query
          required: false
          description: Optional explicit Direct Drive executor id. When set, TokenPilot will not silently fall back to another executor.
          schema:
            type: string
      responses:
        "200":
          description: Git status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GitStatusResponse'
  /api/git/commit:
    post:
      operationId: gitCommit
      summary: Stage public-safe changes and commit
      description: Stages only public-safe changed paths and creates a commit with the given message. Env files, TokenPilot runtime state, logs, and local agent state are not staged by this endpoint.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GitCommitPayload'
      responses:
        "200":
          description: Commit result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GitCommitResponse'
  /api/continuity/projects:
    get:
      operationId: listContinuityProjects
      summary: List continuity projects
      description: Synchronizes allowlisted repo mappings into durable TokenPilot project and workspace identities. Local absolute paths are omitted from the response.
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [active, archived]
      responses:
        "200":
          description: Public-safe project projections
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuityReadResponse'
  /api/continuity/projects/{projectId}:
    get:
      operationId: getContinuityProject
      summary: Read continuity project
      description: Returns one durable project and its public-safe workspace projections.
      security:
        - bearerAuth: []
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Project projection
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuityReadResponse'
  /api/continuity/workspaces/{workspaceId}/snapshot:
    get:
      operationId: getWorkspaceContinuitySnapshot
      summary: Read workspace continuity snapshot
      description: Returns public-safe Git, active writer, task, session, handoff, evidence, and pending approval state for one workspace.
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Workspace continuity snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceContinuitySnapshotResponse'
  /api/continuity/documents:
    get:
      operationId: listDevelopmentDocuments
      summary: List Spec and Plan documents
      description: Returns public-safe document summaries for one workspace without returning Markdown bodies.
      security:
        - bearerAuth: []
      parameters:
        - name: workspaceId
          in: query
          required: true
          schema:
            type: string
        - name: kind
          in: query
          required: false
          schema:
            type: string
            enum: [spec, plan]
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [draft, ready, approved, superseded, archived]
      responses:
        "200":
          description: Document summaries
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DevelopmentDocumentListResponse'
    post:
      operationId: createDevelopmentDocument
      summary: Create Spec or Plan document
      description: Creates one draft Spec or Plan and immutable version 1 using an idempotency key.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DevelopmentDocumentCreatePayload'
      responses:
        "200":
          description: Created or replayed document
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DevelopmentDocumentMutationResponse'
  /api/continuity/documents/{documentId}:
    get:
      operationId: getDevelopmentDocument
      summary: Read Spec or Plan document
      description: Returns one document, its current public-safe Markdown, and append-only version summaries.
      security:
        - bearerAuth: []
      parameters:
        - name: documentId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Document detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DevelopmentDocumentReadResponse'
  /api/continuity/documents/{documentId}/versions/{version}:
    get:
      operationId: getDevelopmentDocumentVersion
      summary: Read immutable Spec or Plan version
      description: Returns one public-safe Markdown version by document id and version number.
      security:
        - bearerAuth: []
      parameters:
        - name: documentId
          in: path
          required: true
          schema:
            type: string
        - name: version
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        "200":
          description: Immutable document version
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DevelopmentDocumentVersionReadResponse'
  /api/continuity/documents/append-version:
    post:
      operationId: appendDevelopmentDocumentVersion
      summary: Append Spec or Plan version
      description: Appends immutable Markdown and returns the document to draft when a ready or approved version changes.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DevelopmentDocumentAppendVersionPayload'
      responses:
        "200":
          description: Appended or replayed version
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DevelopmentDocumentMutationResponse'
  /api/continuity/documents/update-status:
    post:
      operationId: updateDevelopmentDocumentStatus
      summary: Update Spec or Plan lifecycle status
      description: Applies a reviewed lifecycle transition with optimistic revision and idempotency controls.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DevelopmentDocumentStatusPayload'
      responses:
        "200":
          description: Updated or replayed document
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DevelopmentDocumentMutationResponse'
  /api/continuity/tasks/bind-documents:
    post:
      operationId: bindTaskDevelopmentDocuments
      summary: Bind Task Spec and Plan versions
      description: Pins the current immutable Spec and Plan versions after validating kind, ownership, lifecycle state, and Task revision.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaskDocumentBindPayload'
      responses:
        "200":
          description: Bound or replayed Task document state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskDocumentBindResponse'
  /api/continuity/async-jobs/queue:
    post:
      operationId: queueContinuityAsyncJob
      summary: Queue a continuity-bound async Job
      description: Creates one file-backed Codex Runner Job for an active async-agent Session, persists the Job ID as a TokenPilot Runner Runtime Binding, and replays the original result for the same idempotency key.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AsyncJobQueuePayload'
      responses:
        "200":
          description: Job queued or idempotently replayed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AsyncJobQueueResponse'
        "409":
          description: Session, revision, workspace, Runtime Binding, or idempotency conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/continuity/tasks:
    post:
      operationId: createContinuityTask
      summary: Create continuity task
      description: Creates a durable project task. The idempotency key prevents duplicate task creation across REST and MCP retries.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContinuityTaskCreatePayload'
      responses:
        "200":
          description: Created or replayed task
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuityMutationResponse'
  /api/continuity/tasks/submit-review:
    post:
      operationId: submitContinuityTaskReview
      summary: Submit continuity task for review
      description: Finalizes passed required evidence and moves an in-progress or blocked task into review through one idempotent transition.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContinuityTaskSubmitReviewPayload'
      responses:
        "200":
          description: Submitted or replayed task review transition
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuityTaskSubmitReviewResponse'
        "409":
          description: Review evidence requirements are not satisfied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/continuity/tasks/complete:
    post:
      operationId: completeContinuityTask
      summary: Complete continuity task
      description: Completes a review task only after accepted handoff, required evidence, writer, runtime run, and approval checks pass.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContinuityTaskCompletePayload'
      responses:
        "200":
          description: Completed or replayed task transition
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuityTaskCompleteResponse'
        "409":
          description: Completion requirements are not satisfied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /api/continuity/tasks/{taskId}:
    get:
      operationId: getContinuityTask
      summary: Read continuity task
      description: Returns one durable TokenPilot task by id.
      security:
        - bearerAuth: []
      parameters:
        - name: taskId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Task record
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuityReadResponse'
  /api/continuity/sessions/start:
    post:
      operationId: startContinuitySession
      summary: Start development session
      description: Starts a Chat Direct, Codex Session, or Async Agent session and binds it to the expected task revision.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContinuitySessionStartPayload'
      responses:
        "200":
          description: Started or replayed session
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuityMutationResponse'
  /api/continuity/sessions/{sessionId}:
    get:
      operationId: getContinuitySession
      summary: Read development session
      description: Returns one durable TokenPilot development session by id.
      security:
        - bearerAuth: []
      parameters:
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Session record
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuityReadResponse'
  /api/continuity/leases/acquire:
    post:
      operationId: acquireContinuityLease
      summary: Acquire workspace writer lease
      description: Acquires the only active writer lease allowed for a workspace. Another active session receives a conflict.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContinuityLeaseAcquirePayload'
      responses:
        "200":
          description: Acquired or replayed writer lease
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuityMutationResponse'
  /api/continuity/leases/release:
    post:
      operationId: releaseContinuityLease
      summary: Release workspace writer lease
      description: Releases an active writer lease using lease identity, holder identity, revision, and idempotency control.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContinuityLeaseReleasePayload'
      responses:
        "200":
          description: Released or replayed writer lease
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuityMutationResponse'
  /api/continuity/handoffs/prepare:
    post:
      operationId: prepareContinuityHandoff
      summary: Prepare development handoff
      description: Creates a ready handoff checkpoint with goal, progress, pending work, changed files, risks, Git state, evidence, and next action.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContinuityHandoffPreparePayload'
      responses:
        "200":
          description: Ready or replayed handoff
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuityMutationResponse'
  /api/continuity/handoffs/cancel:
    post:
      operationId: cancelContinuityHandoff
      summary: Cancel development handoff
      description: Supersedes one ready handoff with optimistic revision and idempotency controls.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContinuityHandoffCancelPayload'
      responses:
        "200":
          description: Cancelled or replayed handoff
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuityMutationResponse'
  /api/continuity/handoffs/fork:
    post:
      operationId: forkContinuityHandoff
      summary: Fork development handoff
      description: Consumes one ready handoff by creating a child task and target-mode development session in one transaction.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContinuityHandoffForkPayload'
      responses:
        "200":
          description: Forked or replayed handoff
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuityMutationResponse'
  /api/continuity/handoffs/accept:
    post:
      operationId: acceptContinuityHandoff
      summary: Accept development handoff
      description: Accepts one ready handoff with optimistic revision and idempotency controls.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContinuityHandoffAcceptPayload'
      responses:
        "200":
          description: Accepted or replayed handoff
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuityMutationResponse'
  /api/continuity/evidence/record:
    post:
      operationId: recordContinuityEvidence
      summary: Record verification evidence
      description: Records structured verification evidence for a task session and optionally creates a new evidence bundle.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContinuityEvidenceRecordPayload'
      responses:
        "200":
          description: Recorded or replayed evidence
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContinuityMutationResponse'
  /api/runtime/codex/capabilities:
    get:
      operationId: getCodexRuntimeCapabilities
      summary: Read Codex App Server capabilities
      description: Returns a public-safe availability and protocol snapshot. Binary paths and local diagnostics are not exposed.
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Codex runtime capability snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeCapabilitiesResponse'
  /api/runtime/codex/threads:
    get:
      operationId: listCodexRuntimeThreads
      summary: List Codex thread metadata
      description: Lists public-safe Codex thread metadata. Raw cwd, instruction sources, rollout paths, and turn contents are omitted.
      security:
        - bearerAuth: []
      parameters:
        - name: cursor
          in: query
          required: false
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
        - name: workspaceId
          in: query
          required: false
          schema:
            type: string
          description: TokenPilot workspace id. The private workspace path is resolved only inside the local runtime.
        - name: searchTerm
          in: query
          required: false
          schema:
            type: string
            minLength: 1
            maxLength: 500
        - name: archived
          in: query
          required: false
          schema:
            type: boolean
      responses:
        "200":
          description: Public-safe thread list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeThreadListResponse'
  /api/runtime/codex/threads/{threadId}:
    get:
      operationId: getCodexRuntimeThread
      summary: Read Codex thread metadata
      description: Reads one public-safe thread metadata projection. Turn history remains unavailable until TokenPilot defines and verifies a safe projection.
      security:
        - bearerAuth: []
      parameters:
        - name: threadId
          in: path
          required: true
          schema:
            type: string
        - name: includeTurns
          in: query
          required: false
          schema:
            type: boolean
            default: false
          description: Must remain false in the current read-only adapter. True returns CAPABILITY_UNAVAILABLE.
      responses:
        "200":
          description: Public-safe thread metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeThreadReadResponse'
  /api/runtime/codex/sessions/bind:
    post:
      operationId: bindCodexRuntimeSession
      summary: Bind an existing Codex thread
      description: Validates TokenPilot session revision and workspace identity, then binds an existing Codex thread without starting a Codex turn. Repeated requests with the same idempotency key replay the original result.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuntimeSessionMutationPayload'
      responses:
        "200":
          description: Created or replayed runtime binding
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeBindingMutationResponse'
  /api/runtime/codex/sessions/resume:
    post:
      operationId: resumeCodexRuntimeSession
      summary: Resume and bind a Codex thread
      description: Resumes an existing Codex App Server thread and persists the binding with two-phase idempotency. This operation does not start a Codex turn. An uncertain external result remains pending to prevent duplicate resume side effects.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuntimeSessionMutationPayload'
      responses:
        "200":
          description: Resumed or replayed runtime binding
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeBindingMutationResponse'
  /api/runtime/codex/sessions/fork:
    post:
      operationId: forkCodexRuntimeSession
      summary: Fork and bind a Codex thread
      description: Creates a durable non-ephemeral Codex thread fork and binds it to the TokenPilot session with two-phase idempotency. An optional lastTurnId limits the fork boundary. This operation does not start a Codex turn.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuntimeSessionForkPayload'
      responses:
        "200":
          description: Forked or replayed runtime binding
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeBindingMutationResponse'
  /api/runtime/codex/turns/start:
    post:
      operationId: startCodexRuntimeTurn
      summary: Start an explicit Codex turn
      description: Starts one Codex model loop after validating Runtime Binding, revisions, Writer Lease, Git checkpoint, Evidence, and Handoff. Approval is fixed to on-request user review. cwd, model, sandbox, approval, and instruction overrides are unavailable. Retries use two-phase idempotency.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuntimeTurnStartPayload'
      responses:
        "200":
          description: Started or replayed Codex turn
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeTurnStartResponse'
  /api/runtime/codex/turns/interrupt:
    post:
      operationId: interruptCodexRuntimeTurn
      summary: Interrupt an active Codex turn
      description: Explicitly interrupts one active bound Codex turn, releases its Writer Lease, moves the Development Session to handoff-ready, and records an append-only lifecycle event. The operation is idempotent across REST and MCP retries.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuntimeTurnInterruptPayload'
      responses:
        "200":
          description: Interrupted or replayed Codex turn
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeTurnInterruptResponse'
  /api/runtime/codex/approvals/respond:
    post:
      operationId: respondToCodexRuntimeApproval
      summary: Respond to a pending Codex approval
      description: Sends an explicit accept, decline, or cancel decision for one pending command-execution or file-change approval. Session-wide approval and permission escalation are intentionally unavailable. Internal App Server request handles and raw private request parameters are never returned by this API.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RuntimeApprovalRespondPayload'
      responses:
        "200":
          description: Responded or replayed approval decision
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeApprovalRespondResponse'
  /api/runtime/codex/events:
    get:
      operationId: readCodexRuntimeEvents
      summary: Read public-safe Codex runtime events
      description: Reads append-only lifecycle, approval, item, warning, and error events for one TokenPilot Session or Runtime Run. Raw prompts, command output, cwd, file patches, private approval payloads, and App Server request handles are omitted.
      security:
        - bearerAuth: []
      parameters:
        - name: sessionId
          in: query
          required: false
          schema:
            type: string
          description: Filter by TokenPilot Development Session. sessionId or runId is required.
        - name: runId
          in: query
          required: false
          schema:
            type: string
          description: Filter by TokenPilot Runtime Run. sessionId or runId is required.
        - name: afterSequence
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
          description: Return events after this append-only sequence cursor.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
      responses:
        "200":
          description: Public-safe runtime event page
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeEventsResponse'
